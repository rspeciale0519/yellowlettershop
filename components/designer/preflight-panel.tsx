// Phase 13: spec-driven preflight relocated to ./preflight/* (Rule 1 — file
// preserved as a re-export so the sidebar import path stays unchanged).
export { PreflightPanel } from "@/components/designer/preflight/preflight-panel"

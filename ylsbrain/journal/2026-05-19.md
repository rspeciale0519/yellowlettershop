# 2026-05-19

### [21:05] Session crossed UTC midnight — coverage entry (branch: develop · commits: ef9b5be e26f04e 1823ada 7ec7e78)
**Synopsis:** This long session crossed 00:00 UTC during finalization. All substantive work — building the portable `_brain` system end-to-end and adopting YLS as instance #1 + post-task consolidation — is fully recorded in [[journal/2026-05-18]] [20:56] and [21:00]. This entry covers the post-rollover ledger (finalization + bookkeeping commits) so the date-based Stop gate resolves cleanly on the new UTC day.
**What worked:** Recognized the documented UTC/local date-rollover artifact (date-based covering compares the latest journal file's date to the oldest unconsumed ledger date: `b.day >= oldestDay`) manifesting as a real Stop BLOCK when the UTC day advanced while journaling stayed in the prior day's file. Resolved per-protocol by opening today's UTC journal with a covering entry rather than back-dating new work into yesterday's file. Evidence: pre-entry `echo '{}' | node .claude/hooks/stop.js` → exit 2 (BLOCKED, message referenced `journal/2026-05-19.md`); after this entry → expected exit 0 (covering found, watermark advances); `_brain` `BRAIN TEST: ALL GREEN`; instance #0 + instance #1 both `ALL VERIFY GREEN`; YLS `session-start` emits `=== YLS BRAIN ===`; 4/4 `ylsbrain/knowledge/*` still `status: current`.
**What did NOT work:** The date-based covering check does not auto-bridge a UTC-midnight crossing inside a single working session — a known, accepted artifact (recorded in STATE Notes), not a new defect; handled correctly by this per-UTC-day entry. The AL-5 PII heuristic emitted its known benign git-SHA false-positive (non-blocking, documented).
**Artifacts:** `_brain` v0.1.0 (git tag) — engine/CLI/tests + README + instance-#0 protocol (`7ec7e78`), instance #0 dogfooded. YLS instance #1: adoption `ef9b5be`, brain bookkeeping `e26f04e`/`1823ada`. Authoritative narrative + evidence: [[journal/2026-05-18]] [20:56] (build+adoption) and [21:00] (consolidation).
**Next / open:** Project Brain System **v0.1.0 complete** — instances #0 (`_brain/`) and #1 (YLS); all suites GREEN; YLS engine now sync-managed from `_brain`. Out of v1 scope (explicit follow-up): onboarding other projects via `brain init <repo>`. Unchanged: 5 [[knowledge/superseded]] F1–F5 flags still await user confirmation.

### [21:26] Confirm + settle the 5 doc-vs-code flags (F1–F5) (branch: develop · commits: pending)
**Synopsis:** User confirmed all 5 FLAG-TO-USER deltas. Independently re-verified each against current code first (principle D — don't ask the user to rubber-stamp stale findings), then flipped [[knowledge/superseded]] F1–F5 from `provisional, pending confirmation` → **CONFIRMED 2026-05-19** and reconciled the now-stale "flagged/provisional" wording in `features.md`/`roadmap.md`.
**What worked:** Re-verification matched prior findings exactly with zero code drift (consistent with the no-app-drift reconcile). Each claim re-checked at its exact `path:line`; surfaced a sharper truth for F1 (the order-flow validation is `Math.random()` with **no env branch** → environment-independent; the real AccuZip client exists only for the list-builder count/search path, not the order upload) which was folded into the ledger + roadmap. Evidence: `grep`/read of `app/api/accuzip/upload/route.ts:108-159`, `lib/list-builder/list-builder-service.ts:55-56` (`$0.10` hardcoded, **no** `app/api/list-builder/purchase` route), `app/templates/page.tsx:9` (static `templatesData`), `app/dashboard/orders/page.tsx:15-27` (mock `lib/data-structures`), no outbound Mailgun send anywhere; post-edit `brain verify` (YLS instance #1, central engine) → `ALL VERIFY GREEN`; superseded.md = 6× `CONFIRMED 2026-05-19`, 0 `FLAGGED — provisional`.
**What did NOT work:** (none) — re-verification first-pass clean; no contradictions. Side-note (out of scope, not fixed): `roadmap.md` still cites the disposable scratch "dossier §B" in 2 lines — a pre-existing self-containment nit from the knowledge build, flagged not fixed.
**Artifacts:** `ylsbrain/knowledge/superseded.md` (F1–F5 status → CONFIRMED + intro line; F1/F2 `why` sharpened), `features.md` (PARTIAL-items note → confirmed), `roadmap.md` (F1 near-term line: env-independent detail + "Confirmed gap"). NO app code changed — these are brain-knowledge records only. User-confirmed remotely (explained in plain terms; confirmed facts are env-independent so no local check was needed).
**Next / open:** F1–F5 are settled truth; their unbuilt portions remain tracked as near/mid-term in [[knowledge/roadmap]] (AccuZip-real, MelissaData purchase, Mailgun outbound, templates→DB, user orders→live). Optional housekeeping: de-reference the disposable dossier in `roadmap.md` (self-containment). Brain knowledge layer fully reconciled; no provisional items remain.

### [23:41] Global `_brain` self-discovery stanza (Rule 11) (branch: develop · commits: pending)
**Synopsis:** Closed the cross-repo discoverability gap: with explicit user authorization, added a concise **Rule 11** to the user's global `~/.claude/CLAUDE.md` so every future Claude Code session in any repo self-discovers the `_brain` system and auto-honors an installed brain — but **never proactively offers to bootstrap one** (user chose option (a): passive awareness, user-initiated install only).
**What worked:** Honest self-review of the first draft caught a real DRY/staleness flaw (it duplicated the per-project brain protocol that is hook-injected + authoritative in `<vault>/CLAUDE.md`) and a proactive-vs-when-asked ambiguity; the approved v2 *points* at `_brain/README.md`/injected protocol instead of restating, ~12 lines vs ~22. Placed surgically between Rule 10 and the removable `## DONTASK` block (permanent-rules region). Evidence: `grep` confirms order Rule 10 (L372) → Rule 11 (L384) → DONTASK (L404); structure intact; awaited explicit user approval of wording AND of editing the global file before touching it (it is outside-project + a curated user file).
**What did NOT work:** (none) — single targeted Edit, first pass. Accepted limitation recorded in review: Rule 11 assumes the SessionStart hook injects; no global fallback for "`.brain.json` present but nothing injected" (rare; left to the brain's own README/protocol rather than bloat the global file).
**Artifacts:** `~/.claude/CLAUDE.md` Rule 11 (global, user-config — NOT committed to any repo; outside YLS/_brain by nature). No app/_brain code changed. Decision: discoverability is passive-only (option a) — Claude is *equipped*, never *trigger-happy*.
**Next / open:** Cross-repo discovery now solved. Onboarding other projects remains user-initiated (`brain init <repo>`), by design. No open brain threads.

### [23:57] Diagnose 5 failing MCP servers (env, not config) (branch: develop · commits: none — diagnosis + user handoff, no code)
**Synopsis:** User reported "5 MCP tools failing to connect." Root-caused to a PATH gap, not bad config: the npm global-bin dir is absent from PATH so 5 stdio servers launched by bare command names can't be spawned. Fix verified; persistent apply blocked by the auto-mode classifier and handed to the user as a self-run one-liner.
**What worked:** Triaged `claude mcp list` into 3 states — ✓ connected, ! needs-OAuth (Gmail/Calendar/Drive/Vercel/Stripe — expected, not failures), and the real 5 ✗ (git, github, brave, postgres, chrome-devtools). Confirmed each is `stdio` with a bare bin name (`claude mcp get`), the npm packages are globally installed, and the shims exist in `…\AppData\Roaming\npm` while only `…\AppData\Local\nodejs` is on PATH (npx-launched servers work because npx *is* on PATH). Evidence (manual): post-fix proof — prepending the npm bin dir to a throwaway shell PATH made all 5 resolve via `Get-Command` → their `.ps1` shims; package.json `bin` names verified to exactly match the configured commands.
**What did NOT work:** Persisting user PATH via `[Environment]::SetEnvironmentVariable(...,'User')` was denied by the Claude Code auto-mode permission classifier (Unauthorized Persistence / out-of-project per Rule 9; classifier can't see AskUserQuestion approval). Not bypassed — delivered as a `!`-prefixed idempotent command (with PATH backup) for the user to run, plus mandatory full Claude Code restart so MCP servers re-inherit env. `setx` rejected as the mechanism (1024-char truncation).
**Artifacts:** No repo files changed (env-only + user handoff). Decision: PATH-append is the root-cause fix; `npx -y <pkg>` config-rewrite offered as a no-PATH fallback. Security heads-up surfaced to user: GitHub PAT / Brave key / Postgres pw sit in plaintext in `~/.claude.json` (values NOT recorded here per NO-secrets rule). Caveats flagged: chrome-devtools also needs Chrome on `--remote-debugging-port=9222`; github PAT validity never exercised (server never launched).
**Next / open:** Awaiting user to run the PATH one-liner + restart Claude Code, then re-check `claude mcp list`. Out of scope: playwright user-vs-project scope-conflict warning (currently ✓ via project endpoint) — noted, not acted on.

### [00:03] CORRECTION — root cause is stale process env, not a PATH gap (branch: develop · commits: none — diagnosis correction, no code)
**Synopsis:** Supersedes the [23:57] root-cause interpretation. The npm global-bin dir was **already in the persistent User PATH** all along; the user's idempotent fix script reported "Already present" (no-op, wrote no backup). The 5 stdio MCP servers fail solely because the **running Claude Code process holds a stale environment snapshot** captured before that PATH entry existed (Windows fixes process env at launch, never live-reloads). No PATH edit is or was required.
**What worked:** Disambiguated persistent-registry PATH vs process PATH with a 4-way probe. Evidence (manual): `[Environment]::GetEnvironmentVariable('PATH','User')` contains `…\Roaming\npm` = **True**; Machine = False; **current process** `$env:PATH` = **False** (the stale env); simulated fresh Machine+User merge = True and `git-mcp-server.cmd` Test-Path = True → a newly-launched process resolves all 5. Conclusion: fix = clean restart of Claude Code from a freshly-opened terminal / desktop app (not relaunch inside the same stale shell), then `claude mcp list`.
**What did NOT work:** My [23:57] reading conflated process env with persistent PATH — measured Claude Code's stale `$env:PATH` and inferred "PATH misconfigured / needs appending." It did not; the registry value was correct. Lesson (pitfall): on Windows, "command not found for a globally-installed bin" → first compare `GetEnvironmentVariable(...,'User'/'Machine')` against `$env:PATH` before concluding PATH is misconfigured; a stale long-lived host process is the more common cause and the fix is a restart, not an env write. The earlier blocked `SetEnvironmentVariable` denial was therefore moot (the change was unnecessary anyway).
**Artifacts:** `docs/temp/fix-npm-path.ps1` (idempotent; correctly no-op'd — kept as reference, harmless). No repo/env changes. Decision: do NOT modify PATH; remediation is operational (restart). Unchanged caveats: chrome-devtools also needs Chrome on `--remote-debugging-port=9222`; github PAT validity still unexercised.
**Next / open:** Awaiting user clean-restart + `claude mcp list` verification. If servers still ✗ after a genuinely fresh-env restart → deeper cause (Claude Code launched by a parent/launcher with a reduced env, or PATH-length truncation at process creation) — investigate then. Candidate pitfall skill if it recurs: `skills/build-*` "stale host-process env vs persistent PATH on Windows".

### [00:22] RESOLVED — 5 MCP servers fixed via npx rewrite (true root cause: VS Code-inherited reduced PATH) (branch: develop · commits: none — user-global MCP config, outside repo)
**Synopsis:** User restarted Claude Code fully; the 5 still failed (`brave -32000`) — disproving the [00:03] "stale-env / restart fixes it" conclusion. Systematic-debugging skill invoked. True root cause traced: **Claude Code runs inside the VS Code integrated terminal** (`claude.exe` ← `pwsh.exe` ← VS Code `shellIntegration.ps1`); the VS Code process tree carries a **reduced 38/118-entry PATH that lacks `%APPDATA%\npm`** (it predates that User-PATH entry and GUI apps don't re-read it), so the 5 bare-command stdio servers can't be spawned. Fixed by rewriting all 5 to `cmd /c npx -y <pkg>` (launch-context-independent, matches the already-working playwright/context7). **All 5 → ✓ Connected, verified.**
**What worked:** Phase-1 reproduction nailed it: `git-mcp-server` (bare) → spawn ENOENT "system cannot find the file specified"; `…\Roaming\npm\git-mcp-server.cmd` (abs) and `cmd /c npx -y @mseep/git-mcp-server` → both start a healthy stdio MCP. Parent-process walk (`Win32_Process` CIM) exposed the VS Code shell-integration ancestor + the 38-vs-118 PATH-entry gap (`node`/`npx` resolve from `…\Local\nodejs` which IS inherited → that's why npx/HTTP servers always worked). Canary-first discipline: reconfigured `git` only → ✓ before touching the rest. Env/secrets preserved by reading them programmatically (`ConvertFrom-Json -AsHashtable`) and re-supplying via `claude mcp add -e` (values never printed). Evidence (manual): `claude mcp list` post-fix = git/github/brave/postgres/chrome-devtools all **✓ Connected**; all prior-green servers unaffected; backup `C:\Users\rob\.claude.json.bak-20260519-001813` byte-identical (139332 B) for rollback.
**What did NOT work:** (1) [23:57] "PATH not on disk" and [00:03] "stale env, restart fixes" were both **wrong/incomplete** — superseded by this entry; the operative cause is the VS Code/GUI process-tree environment, not Claude Code's own process and not a missing registry entry. (2) First `claude mcp add` issued via the **Bash tool** mangled `/c` → `C:/` (Git-Bash/MSYS POSIX→Windows arg conversion) producing `cmd C:/ npx …`; redoing it from the **PowerShell** tool fixed it. (3) `ConvertFrom-Json` (default) threw on the config — file has case-only-duplicate project-path keys; `-AsHashtable` is required to read it.
**Artifacts:** `~/.claude.json` (user-global, outside repo — NOT committed) — 5 servers' command/args rewritten to `cmd /c npx -y <pkg>`, env untouched. Backup: `C:\Users\rob\.claude.json.bak-20260519-001813`. Memories added: [[mcp-stdio-servers-need-npx-wrapper]] (project), [[bash-tool-mangles-windows-c-flag]] (feedback). `docs/temp/fix-npm-path.ps1` now obsolete (PATH edit was never the fix) — harmless, left as-is. No app/_brain code touched.
**Next / open:** MCP layer fully green and launch-independent (any terminal / fresh login / VS Code all work). Optional housekeeping (pre-existing, NOT done — out of scope, playwright currently ✓): `playwright` user-vs-project scope-conflict warning — user may `claude mcp remove playwright -s user`. Consolidation flagged overdue at session start — offer post-task.

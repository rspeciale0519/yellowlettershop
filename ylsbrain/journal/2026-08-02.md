# Journal — 2026-08-02

### [00:20] Redstone outreach email + branch push   (branch: feature/vendor-fulfillment · pushed 0958a71..9a9de3d)

**Synopsis:** Follow-on to the 2026-08-01 Redstone build. Pushed the three
local commits (money-moment confirmations, Redstone Phase 1, brain docs) so PR
#24 reflects them, and drafted the outreach email to Redstone that the probing
established is now the critical path. Owner then asked for the email rewritten
with no em dashes, no markdown, and no unnecessary mid-sentence line wrapping,
so it was reissued as plain text.

**What worked:** Leading the email with our own hypothesis rather than a
complaint. It states that spec §4.1/§4.2 say Redstone provisions the endpoint
per customer, guesses ours is not yet provisioned, and explicitly allows that
we may have it wrong. It also lists all four payload shapes already tried
(flat, `{"Order":{…}}`-wrapped, with `seeds`, form-encoded) so their first
reply cannot just be "did you try wrapping it", and cites the test order IDs
`YLS-APITEST-20260801-A`–`E` so their team can pull our attempts from their
logs. On webhook auth it proposes a shared secret or HMAC-SHA256 and defers to
whatever they already run for other integrations.

Evidence: push succeeded `0958a71..9a9de3d`. Format compliance verified
mechanically, not by eye — `grep` for U+2014/U+2013 returned 0, and a grep for
`**`, leading `#`, backticks, leading `- `, and `---` returned no matches.
Paragraphs are single unbroken lines so the text reflows in the mail client
rather than carrying a hard 76-column wrap.

**What did NOT work:** The first draft was written to `docs/temp/…​.md`, which
`git add -A` silently ignored — `.gitignore:74` excludes `temp/`. Not a bug
(Rule 5 puts working docs there by design, and the repo excludes them), but
worth remembering: **anything written to `docs/temp/` will never be committed**,
so it cannot be the only copy of something that matters. The email draft is
therefore local-only on this machine.

**Artifacts:**
- `docs/temp/redstone-api-integration-email.txt` (plain text, gitignored,
  local-only; superseded `.md` first draft removed)
- Branch `feature/vendor-fulfillment` pushed; PR #24 now carries fb8e19e,
  a0f7d56, 9a9de3d

**Next:** Owner sends the email (needs recipient name, phone, email filled in).
Blocking answer is question 1, whether our endpoint is provisioned.

---

### [16:10] Pre-merge browser smoke test of PR #24 + two fixes   (branch: feature/vendor-fulfillment · commits: 3c71149, bb3ef5a)

**Synopsis:** Archived the fabricated `dev-docs/api-redstone.md` to
`archive/api-redstone-fabricated-2026-08/` with a README recording the
falsehood-by-falsehood evidence, and updated every reference (dev-docs README,
integrations-and-data, implementation-status §5/§8b/doc-map, and the header
comment in `redstone-core.ts`). Then ran the browser click-through that had
been outstanding since the CDP wedge two sessions ago, found two real display
bugs, fixed both, and re-verified.

**What worked:** CDP was healthy this session, so the whole customer money path
was exercised for real against the local stack in Stripe test mode.

Verified in-browser, with evidence:
- **Proof page (the risky screen).** Reject dialog opens, cancels inert, order
  untouched. Approve dialog shows `$118.00` in amber with "Charged now — this
  cannot be undone" and three consequences; confirming captured for real →
  `status=processing`, `payment_status=captured`, `amount_captured=118`
  (dollars, not cents), `captured_at` + `proof_approved_at` both stamped,
  timeline advanced to "In production", zero console errors.
- **Review step.** Exactly two checkboxes with the owner's agreed wording.
  Continue to Payment stayed disabled at 0 checked AND at 1 checked, enabling
  only at 2 — the UI/gate divergence that motivated the change is genuinely
  gone.
- **Payment step.** Dialog reads "Place a hold and submit your order?", `$1.18`,
  "Held on VISA ····4242 — not charged yet". Cancel inert; confirm submitted
  order `12e011fd` with `payment_status=authorized`.
- **Manual entry** retained two recipients across successive adds — the wipe
  bug stays fixed. Design step reported "2 personalization variables detected",
  clearing an older "0 variables" nit.
- Auto-dispatch refused the seeded order with `Order has no recipients to mail`
  — a seeding artifact, but it proved the non-fatal design: capture and customer
  approval both succeeded and the failure only logged for admin follow-up.

**Two bugs found and fixed (bb3ef5a):**
1. Pricing breakdown rendered `Base printing (1.180 × 1)` against `$0.45` — it
   labelled the base-printing line with `pricePerPiece`, which is the ALL-IN
   rate (`total / pieces`, pricing.ts:146). The line contradicted itself on the
   screen where the customer approves the charge. Now shows the printing unit
   rate, with the all-in rate given its own line under Total.
2. "Add New Payment Method" still carried an `ExternalLink` icon promising a new
   tab; it opens an in-page dialog since this branch's card-entry work. → `Plus`.

**What did NOT work / limits:**
- **The icon swap (fix 2) was NOT verified by eye** — Fast Refresh resets the
  in-memory wizard to step 1 on every edit, and re-walking six steps to look at
  one icon was disproportionate. Verified by typecheck + no remaining
  `ExternalLink` references only.
- Walking the wizard required blanking `ACCUZIP_API_KEY`: with a key present,
  `batchValidateRecords` calls the real AccuZip endpoint, which fails and marks
  every address undeliverable (0% delivery rate), hard-blocking Continue.
  **`.env.local` was backed up and byte-for-byte restored** (hash-compared).
- The design gate genuinely requires a saved design; reached it via the
  designer's own documented `localStorage['yls.pendingOrderDesign']` + focus
  contract rather than driving the designer UI.
- Radix components (Select options, Tabs) ignore a bare `.click()`; they need
  dispatched pointerdown/pointerup or the MCP click tool.
- Pre-existing friction noted, NOT fixed (out of scope): manual entry requires
  clicking Auto-Map even though its records already use canonical field names,
  and the payment step leaves a lone saved card unselected so the authorize
  button starts disabled.

**Artifacts:**
- `archive/api-redstone-fabricated-2026-08/{api-redstone.md,README.md}`
- `components/orders/steps/ReviewApprovalStep.tsx`,
  `components/orders/steps/payment/PaymentMethodList.tsx`
- Gates: typecheck:full 0, 258 tests, build exit 0.

**Next:** PR #24 is smoke-verified and, in my judgement, mergeable. Remaining
owner calls: run `/code-review ultra 24` first (roughly half the branch — card
entry, confirmations, Redstone — has never been reviewed), then merge to
`develop`. Then send the Redstone email; nothing on the Redstone path can move
until they confirm our endpoint is provisioned.

---

### [17:40] Ultrareview findings on PR #24 — 8 fixed   (branch: feature/vendor-fulfillment · commits: none yet, working tree)

**Synopsis:** `/ultrareview 24` returned 8 findings (3 normal, 5 nit). All 8 were
re-verified against the code before acting; all 8 were real. Fixed every one,
plus a ninth defect of the same class found while fixing the refund path.

**What worked:** Verifying each finding independently instead of trusting the
report. Two things came out of that: (a) the review's `wc -l 426` for
dispatch-service.ts was right and my first count (`Measure-Object -Line` = 377)
was wrong — that cmdlet does not count blank lines, so never use it for a LOC
rule; (b) the claimed reachability held up in every case, including the
partial-refund UI path (`order-payment-actions.tsx:145` advertises it).

The security fixes:
- **Cross-tenant PII (IDOR).** `loadRecipients` queried `mailing_list_records`
  by a customer-supplied `selectedListId` using the service role (RLS bypassed)
  with no ownership check; `/api/orders/submit` accepts `orderState` as
  `z.record(z.unknown())` and stores it verbatim, so the id is fully attacker-
  controlled. Ported the existing gate from `app/api/accuzip/upload/route.ts`
  into a new module. Principal is `order.created_by`, NOT the actor — an admin
  re-dispatching must not lend privileges to a list the customer cannot read.
- **CSV formula injection (CWE-1236).** `csvCell` escaped only `",\n\r`, so a
  recipient first_name of `=WEBSERVICE("http://…"&B2)` reached the vendor's
  Excel intact. Added `neutralizeSpreadsheetFormula` in dispatch-core, applied
  in both CSV builders. In `sanitizeRedstoneCell` it must run LAST — the
  existing `["',]` strip would otherwise eat the guard apostrophe.

The money fix: `refundPayment` overwrote `amount_refunded` with the current
refund's amount, but the column is declared cumulative
(`20260801000000_orders_refund_columns.sql:25`), so a second partial refund
erased the first and admin revenue (captured − refunded) over-reported by the
difference. Extracted `resolveRefundState` as a pure core (matching
dispatch-core/analytics-core/redstone-core) so the arithmetic is testable.

**Ninth defect, not in the review:** `refundOrder` set `status: 'cancelled'` on
ANY refund, so a $1 goodwill refund on a $100 order cancelled an order that
still mails. Now gated on `isFullRefund`.

Evidence: `npm run typecheck:full` 0 errors; `npm test` 269 passing (was 258,
+11 new); `npm run build` exit 0; `npx eslint` on all 10 changed files → 0
errors, 1 pre-existing `any` warning. All touched files verified ≤350 LOC.

**What did NOT work:** First CAS implementation used `.update(v, {count:'exact'})`
and compared `count !== 1`. Rejected before running: that count is derived from
a `Content-Range` header, and if it ever came back null EVERY admin status
update would throw. Switched to `.select('id')` after the filters and checked
the returned rows — same guarantee, no header dependency.

**Artifacts:**
- New: `lib/fulfillment/dispatch-recipients.ts` (auth gate),
  `lib/fulfillment/dispatch-status.ts` (CAS), `lib/payments/refund-core.ts`,
  `tests/payments/refund-core.test.ts`
- dispatch-service.ts 426 → 264 LOC (both extractions), now inside the rule
- `updateDispatchStatus` moved → importers updated (`index.ts`, admin route)

**Next / open:** Not committed — owner's call. **Tripwire on the auth gate:** it
requires `mailing_lists.created_by === order.created_by` OR a shared
`team_id`. A team-shared list whose `team_id` is NULL will now be refused with
"refusing to dispatch" where it previously dispatched. Same rule AccuZip upload
already enforces, so a list that validates can dispatch — but if a real dispatch
starts failing that way, that NULL `team_id` is the first thing to check.

---

### [22:26] PR #24 merged to develop + Vercel project identity resolved   (branch: develop · merge commit: dca90eb)

**Synopsis:** Merged PR #24 (merge commit, matching how earlier PRs landed and
preserving the SHAs this journal cites). Before merging, chased a red
`Vercel – yellowlettershop` check that looked like a regression from my own
commits; it was not.

**What worked:** Refusing to merge on the red check, then resolving it with
evidence rather than a guess. Decisive call: `get_deployment` on
`dpl_9jTg7y2zKtmzh9PC6Y3Htpsu4Hdb` returned `githubCommitSha =
cdb227478c08…` (my exact commit) with `state: READY` — proving the real project
built my code fine.

**THE GOTCHA (this is the reusable part): this repo is connected to THREE Vercel
projects, two of which are both named `yellowlettershop`.**
- `yellowlettershop` / prj_snSyPlSbgjz6sd6hql7JTdfm9mt2 / team_9aYvnhHwLiazNz7OoE8BsRVC
  (slug `robs-projects-c72886ba`) — owns **app.yellowlettershop.com**, matches
  `.vercel/project.json`, builds green. **This is the real one.**
- `yls` / prj_zL3YsTaoL8oMUZ4iiACnnsX0JUTu / same team — no custom domain, fails
  on EVERY commit with `@supabase/ssr: Your project's URL and API key are
  required` at `/api/access-control/activity`. Missing env vars, not code.
- A second `yellowlettershop` on a **different Vercel account**
  (slug `robs-projects-d8ad5232`, team_NMMbkI0495NyEpzRM4JqubxT) — this login is
  403 for that scope.

Because GitHub keys commit statuses by **context string**, and both
`yellowlettershop` projects post the identical context `Vercel – yellowlettershop`,
they overwrite each other. Whichever reports last wins. That is why the check
was green on b8836b5 and red on cdb2274 for functionally identical code — a
reporting race, not a regression.

**What did NOT work:** I first read "green on the previous commit, red on mine"
as proof my commits broke the deploy, and said so. Wrong inference: it assumed
one publisher per status context. **Rule for next time — before attributing a
red Vercel check to a code change, resolve the deployment to its
`githubCommitSha` and confirm which project/account published it.** A clean
local `next build` disagreeing with a red Vercel check is a signal to check
identity/env, not to assume a Linux-only build break.

Also note `npx vercel inspect` was not available (CLI not installed) and the
Vercel MCP is authorized for `robs-projects-c72886ba` only — reading the
d8ad5232 project's logs still requires the owner.

**Artifacts:** merge commit dca90eb on develop; PR #24 MERGED 22:26Z.
Branch `feature/vendor-fulfillment` deliberately NOT deleted (owner's call).

**Next / open:** (1) Decide the fate of the two duplicate Vercel projects — the
`yls` project and the d8ad5232 `yellowlettershop` produce a permanent red X on
every PR and trained us to distrust the checks. Disconnect them from the repo,
or fix their env vars if one is meant to be staging. (2) Redstone still blocked
on their endpoint-provisioning reply. (3) Live smoke of the new dispatch auth
gate against a real order.

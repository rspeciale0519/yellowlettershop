# Journal — 2026-08-02

### [00:20] Redstone outreach email + branch push   (branch: feature/vendor-fulfillment · pushed 0958a71..9a9de3d)

**Synopsis:** Follow-on to the 2026-08-01 Redstone build. Pushed the three
local commits (money-moment confirmations, Redstone Phase 1, brain docs) so PR
#24 reflects them, and drafted the outreach email to Redstone that the probing
established is now the critical path. Owner then asked for the email rewritten
with no em dashes, no markdown, and no unnecessary mid-sentence line wrapping,
so it was reissued as plain text.

**What worked:** Leading the email with our own hypothesis rather than a
complaint. It states that spec §4.1/§4.2 say Redstone provisions the endpoint
per customer, guesses ours is not yet provisioned, and explicitly allows that
we may have it wrong. It also lists all four payload shapes already tried
(flat, `{"Order":{…}}`-wrapped, with `seeds`, form-encoded) so their first
reply cannot just be "did you try wrapping it", and cites the test order IDs
`YLS-APITEST-20260801-A`–`E` so their team can pull our attempts from their
logs. On webhook auth it proposes a shared secret or HMAC-SHA256 and defers to
whatever they already run for other integrations.

Evidence: push succeeded `0958a71..9a9de3d`. Format compliance verified
mechanically, not by eye — `grep` for U+2014/U+2013 returned 0, and a grep for
`**`, leading `#`, backticks, leading `- `, and `---` returned no matches.
Paragraphs are single unbroken lines so the text reflows in the mail client
rather than carrying a hard 76-column wrap.

**What did NOT work:** The first draft was written to `docs/temp/…​.md`, which
`git add -A` silently ignored — `.gitignore:74` excludes `temp/`. Not a bug
(Rule 5 puts working docs there by design, and the repo excludes them), but
worth remembering: **anything written to `docs/temp/` will never be committed**,
so it cannot be the only copy of something that matters. The email draft is
therefore local-only on this machine.

**Artifacts:**
- `docs/temp/redstone-api-integration-email.txt` (plain text, gitignored,
  local-only; superseded `.md` first draft removed)
- Branch `feature/vendor-fulfillment` pushed; PR #24 now carries fb8e19e,
  a0f7d56, 9a9de3d

**Next:** Owner sends the email (needs recipient name, phone, email filled in).
Blocking answer is question 1, whether our endpoint is provisioned.

---

### [16:10] Pre-merge browser smoke test of PR #24 + two fixes   (branch: feature/vendor-fulfillment · commits: 3c71149, bb3ef5a)

**Synopsis:** Archived the fabricated `dev-docs/api-redstone.md` to
`archive/api-redstone-fabricated-2026-08/` with a README recording the
falsehood-by-falsehood evidence, and updated every reference (dev-docs README,
integrations-and-data, implementation-status §5/§8b/doc-map, and the header
comment in `redstone-core.ts`). Then ran the browser click-through that had
been outstanding since the CDP wedge two sessions ago, found two real display
bugs, fixed both, and re-verified.

**What worked:** CDP was healthy this session, so the whole customer money path
was exercised for real against the local stack in Stripe test mode.

Verified in-browser, with evidence:
- **Proof page (the risky screen).** Reject dialog opens, cancels inert, order
  untouched. Approve dialog shows `$118.00` in amber with "Charged now — this
  cannot be undone" and three consequences; confirming captured for real →
  `status=processing`, `payment_status=captured`, `amount_captured=118`
  (dollars, not cents), `captured_at` + `proof_approved_at` both stamped,
  timeline advanced to "In production", zero console errors.
- **Review step.** Exactly two checkboxes with the owner's agreed wording.
  Continue to Payment stayed disabled at 0 checked AND at 1 checked, enabling
  only at 2 — the UI/gate divergence that motivated the change is genuinely
  gone.
- **Payment step.** Dialog reads "Place a hold and submit your order?", `$1.18`,
  "Held on VISA ····4242 — not charged yet". Cancel inert; confirm submitted
  order `12e011fd` with `payment_status=authorized`.
- **Manual entry** retained two recipients across successive adds — the wipe
  bug stays fixed. Design step reported "2 personalization variables detected",
  clearing an older "0 variables" nit.
- Auto-dispatch refused the seeded order with `Order has no recipients to mail`
  — a seeding artifact, but it proved the non-fatal design: capture and customer
  approval both succeeded and the failure only logged for admin follow-up.

**Two bugs found and fixed (bb3ef5a):**
1. Pricing breakdown rendered `Base printing (1.180 × 1)` against `$0.45` — it
   labelled the base-printing line with `pricePerPiece`, which is the ALL-IN
   rate (`total / pieces`, pricing.ts:146). The line contradicted itself on the
   screen where the customer approves the charge. Now shows the printing unit
   rate, with the all-in rate given its own line under Total.
2. "Add New Payment Method" still carried an `ExternalLink` icon promising a new
   tab; it opens an in-page dialog since this branch's card-entry work. → `Plus`.

**What did NOT work / limits:**
- **The icon swap (fix 2) was NOT verified by eye** — Fast Refresh resets the
  in-memory wizard to step 1 on every edit, and re-walking six steps to look at
  one icon was disproportionate. Verified by typecheck + no remaining
  `ExternalLink` references only.
- Walking the wizard required blanking `ACCUZIP_API_KEY`: with a key present,
  `batchValidateRecords` calls the real AccuZip endpoint, which fails and marks
  every address undeliverable (0% delivery rate), hard-blocking Continue.
  **`.env.local` was backed up and byte-for-byte restored** (hash-compared).
- The design gate genuinely requires a saved design; reached it via the
  designer's own documented `localStorage['yls.pendingOrderDesign']` + focus
  contract rather than driving the designer UI.
- Radix components (Select options, Tabs) ignore a bare `.click()`; they need
  dispatched pointerdown/pointerup or the MCP click tool.
- Pre-existing friction noted, NOT fixed (out of scope): manual entry requires
  clicking Auto-Map even though its records already use canonical field names,
  and the payment step leaves a lone saved card unselected so the authorize
  button starts disabled.

**Artifacts:**
- `archive/api-redstone-fabricated-2026-08/{api-redstone.md,README.md}`
- `components/orders/steps/ReviewApprovalStep.tsx`,
  `components/orders/steps/payment/PaymentMethodList.tsx`
- Gates: typecheck:full 0, 258 tests, build exit 0.

**Next:** PR #24 is smoke-verified and, in my judgement, mergeable. Remaining
owner calls: run `/code-review ultra 24` first (roughly half the branch — card
entry, confirmations, Redstone — has never been reviewed), then merge to
`develop`. Then send the Redstone email; nothing on the Redstone path can move
until they confirm our endpoint is provisioned.

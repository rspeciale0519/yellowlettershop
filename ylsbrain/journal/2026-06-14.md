# yls brain — journal 2026-06-14

### [00:00] /fewer-permission-prompts — read-only allowlist analysis (apply BLOCKED by auto-mode)
Synopsis: Ran the /fewer-permission-prompts workflow — scanned the 50 most-recently-modified
transcripts across all projects, ranked read-only tool calls, and proposed an 11-entry allowlist
for `.claude/settings.json`. Writing the settings edit was DENIED by auto-mode's self-modification
classifier (Claude granting itself permissions needs explicit owner approval), so the list was
handed to the owner to apply.

What worked + Evidence:
- No `jq` on PATH (Git-Bash mingw64); fell back to a Python parser (`docs/temp/scan_perms.py`)
  that reads `~/.claude/projects/*/*.jsonl`, extracts `tool_use` entries, and counts Bash
  leading-token pairs + full MCP tool names. Output: 50 files scanned, top Bash `cd`(624)/`git add`(27),
  top MCP `evaluate_script`(410)/`click`(218).
- Filter correctly excluded arbitrary-exec (`evaluate_script`, `npm run`, `npx tsc`, `node -e`,
  `python -c`, `mcp__postgres__query` = arbitrary SQL), mutating browser tools (click/fill/navigate),
  and auto-allowed builtins (cd/cat/ls/git status). De-duped 4 chrome read-only tools already present
  in `.claude/settings.local.json`.
- Final list (new value, not already allowed): chrome-devtools wait_for / list_network_requests /
  get_network_request; vercel get_project / get_deployment / get_runtime_logs / list_deployments;
  brave_web_search; firecrawl_search; `Bash(git fetch *)`; `Bash(nslookup *)`.

What did NOT work:
- `Edit` on `.claude/settings.json` returned auto-mode denial: "Editing settings.json to add
  agent-chosen permission allow rules is Self-Modification." Expected guardrail — surfaced the JSON
  block to the owner instead of working around it.
- First jq-based extraction silently produced 0 lines (jq absent); confirmed via `which jq`.

Artifacts: `docs/temp/scan_perms.py` (transcript scanner); proposed `permissions.allow` block in
the chat response (11 entries). `.claude/settings.json` UNCHANGED (still hooks-only, no permissions key).

Next: owner to approve/apply the allowlist block to `.claude/settings.json` (top-level key beside
`hooks`); on approval, retry the Edit. No code/app changes — production-readiness work untouched.

### [00:05] no-op
Journal entry for the /fewer-permission-prompts analysis already recorded above; nothing further done.

### [00:10] Diagnose Supabase MCP "Unrecognized client_id" auth failure   (branch: feature/production-readiness · commits: none)
**Synopsis:** Owner reported the hosted Supabase MCP re-auth link returns `{"message":"Unrecognized client_id"}`. Diagnosed as a stale OAuth Dynamic Client Registration, not connectivity; gave in-app fix + offered surgical credential-store cleanup (advice only, no changes made).
**What worked:** Isolated the failure layer before prescribing. Evidence: `curl -so /dev/null -w "%{http_code}" https://mcp.supabase.com/mcp` → **401** (server reachable, just unauthenticated), so the error is purely the authorize endpoint rejecting a cached, server-invalidated `client_id`. Also confirmed Supabase MCP is plugin-provided, NOT in project `.mcp.json` (which holds only playwright + stripe) — so the fix path is `/mcp` → supabase → Clear authentication → Authenticate (forces fresh DCR), since plain reauth reuses the dead client_id.
**What did NOT work:** (none) — diagnostic only; no fix applied yet (awaiting owner confirmation to touch `~/.claude/.credentials.json` / `~/.claude.json`, which are outside the project per the directory-restriction rule).
**Artifacts:** `.mcp.json` (read, unchanged); root cause = stale OAuth DCR client_id cached in Claude Code credential store. Fallback remedy: remove only the supabase MCP OAuth entry (with backup) to force re-registration.
**Next / open:** Owner to try the `/mcp` Clear-auth → Authenticate path; if it persists, owner to OK surgically clearing the stale supabase entry from `~/.claude` credential files. No app/code changes.

### [10:30] Supabase key-leak remediation: rotate to new sb_ keys + disable/revoke legacy (browser-driven)   (branch: feature/production-readiness · commits: none yet)
**Synopsis:** Found the old Supabase `service_role` JWT + a PAT committed in pushed git history (commit `5cb2199`, `.claude/settings.local.json`). With owner authorization, migrated prod off legacy keys to the new `sb_publishable_`/`sb_secret_` keys (zero-downtime), then disabled+revoked the legacy keys so the leaked JWT is permanently dead. Leaked PAT was already dead.
**What worked:** Zero-downtime migration sequence (swap env to already-existing new keys → redeploy → verify → THEN revoke). Evidence (manual, all observed):
- New keys valid pre-revoke: REST `user_profiles?select=id&limit=1` → publishable `[]|200` (anon RLS), secret `[{id…}]|200` (bypass).
- Prod redeploy `dpl_GHQPqaHx2pukkN7Jh44CvW28hPCy` reached `readyState:READY`, aliased to app.yellowlettershop.com (Vercel get_deployment).
- Post-revoke: leaked old service_role → `{"message":"Legacy API keys are disabled"}`; new secret/publishable still `200`; leaked PAT `sbp_v0_8ff3…` → Management API `401`.
- Live login page renders, **0 console errors** before AND after revoke.
- Secret never entered the transcript: copied via dashboard "Copy", moved to `.env.local`/Vercel via clipboard (`Get-Clipboard`) + Ctrl+V; verified by length/prefix only.
**What did NOT work:** (1) Vercel `fill` APPENDS to a non-empty field → corrupted SUPABASE_ANON_KEY value once; fix = click→Ctrl+A→Delete→fill, verify via evaluate before save. (2) Editing an "All Environments" var that overlaps Development-scoped entries 409s ("already exists"); fix = retarget to "Production and Preview" (uncheck Development). (3) Supabase blocks revoking the HS256 secret until legacy API keys are DISABLED first — so order is disable → revoke. (4) Checkbox/menu clicks timed out in the virtualized Vercel list → used `evaluate_script` `.click()`.
**Artifacts:** `.env.local` (new key values; backup `.env.local.bak` left, gitignored — contains live secrets, safe to delete). `.gitignore` += `.claude/settings.local.json`, `.env.local.bak`. `.claude/settings.local.json` `git rm --cached` (untracked). Vercel prod env: SERVICE_ROLE_KEY=sb_secret, NEXT_PUBLIC_ANON=sb_publishable. memory:[[project_supabase_key_rotation]]. NO commits yet.
**Next / open:** (a) OPEN: owner must still **rotate the service_role conceptually-done but** — actually done. (b) Vercel **Development + Preview/develop** scoped supabase entries still hold dead legacy values → preview/dev deploys break until updated (non-prod). (c) Deferred git tasks: commit the hygiene+housekeeping bundle (+ optional history purge — now low-priority since leaked key is dead). (d) Owner may delete `.env.local.bak`.

### [11:10] Finish key rotation: Vercel dev/preview env + git commit/PR   (branch: chore/secret-hygiene-and-tooling · commits: `7caa885`)
**Synopsis:** Completed the two follow-ups from [10:30]: updated the remaining non-prod Vercel env entries to the new keys, and committed/pushed the hygiene+tooling bundle as PR #12.
**What worked:** All envs now on new keys. Evidence (manual, observed each "Updated just now" + no save errors): SUPABASE_SERVICE_ROLE_KEY Development + Preview/develop → `sb_secret_` (clipboard paste); NEXT_PUBLIC_SUPABASE_ANON_KEY Development + Preview/develop → `sb_publishable_` (`Set-Clipboard` + paste). Bare `SUPABASE_ANON_KEY` dev/preview left legacy (unused by code, now-dead, harmless). Git: pre-commit staged-diff secret scan showed only `-` (removed) leaked lines, zero `+` additions; commit `7caa885` (10 files), pushed, **PR #12 → main** (open, NOT merged — awaiting owner).
**What did NOT work:** (none new) — same Vercel quirks as [10:30] (fill-appends → focus+Ctrl+A+Delete+paste; virtualized list → search-filter + evaluate clicks).
**Artifacts:** PR #12 (https://github.com/rspeciale0519/yellowlettershop/pull/12), commit `7caa885`, branch `chore/secret-hygiene-and-tooling`.
**Next / open:** Owner to (a) merge PR #12 (will auto-trigger a prod redeploy — harmless), (b) optionally delete `.env.local.bak`, (c) history purge still optional/low-priority.

### [11:25] Harden auto-approve allowlist (push-review findings)   (branch: chore/secret-hygiene-and-tooling · commits: `5271f85`)
**Synopsis:** Automated push security review flagged 3 prompt-injection-amplification risks in the `.claude/settings.json` allowlist (from the earlier /fewer-permission-prompts run). Owner approved tightening; applied + pushed to PR #12.
**What worked:** Narrowed the allowlist. Evidence (manual: Edit applied — note auto-mode did NOT block *removing/narrowing* permissions, unlike *granting* which it blocked on [00:00]; commit `5271f85` 1 file +2/-4, pushed): dropped `Bash(nslookup *)` (DNS exfil) + `mcp__brave__brave_web_search` + `mcp__firecrawl__firecrawl_search` (search = exfil egress; breaks read+exfiltrate pipeline); narrowed `Bash(git fetch *)` → `Bash(git fetch origin)` + `Bash(git fetch origin *)` (blocks `ext::`/`file://`/`--upload-pack` arbitrary exec). Kept chrome/vercel read tools + wait_for auto-approved.
**What did NOT work:** (none).
**Artifacts:** `.claude/settings.json`, commit `5271f85` on PR #12. Lesson: auto-mode self-modification guard blocks GRANTING perms, allows TIGHTENING.
**Next / open:** Same as [11:10] — owner to merge PR #12. Web searches + nslookup will now prompt (intended).

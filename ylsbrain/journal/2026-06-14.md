# yls brain — journal 2026-06-14

### [00:00] /fewer-permission-prompts — read-only allowlist analysis (apply BLOCKED by auto-mode)
Synopsis: Ran the /fewer-permission-prompts workflow — scanned the 50 most-recently-modified
transcripts across all projects, ranked read-only tool calls, and proposed an 11-entry allowlist
for `.claude/settings.json`. Writing the settings edit was DENIED by auto-mode's self-modification
classifier (Claude granting itself permissions needs explicit owner approval), so the list was
handed to the owner to apply.

What worked + Evidence:
- No `jq` on PATH (Git-Bash mingw64); fell back to a Python parser (`docs/temp/scan_perms.py`)
  that reads `~/.claude/projects/*/*.jsonl`, extracts `tool_use` entries, and counts Bash
  leading-token pairs + full MCP tool names. Output: 50 files scanned, top Bash `cd`(624)/`git add`(27),
  top MCP `evaluate_script`(410)/`click`(218).
- Filter correctly excluded arbitrary-exec (`evaluate_script`, `npm run`, `npx tsc`, `node -e`,
  `python -c`, `mcp__postgres__query` = arbitrary SQL), mutating browser tools (click/fill/navigate),
  and auto-allowed builtins (cd/cat/ls/git status). De-duped 4 chrome read-only tools already present
  in `.claude/settings.local.json`.
- Final list (new value, not already allowed): chrome-devtools wait_for / list_network_requests /
  get_network_request; vercel get_project / get_deployment / get_runtime_logs / list_deployments;
  brave_web_search; firecrawl_search; `Bash(git fetch *)`; `Bash(nslookup *)`.

What did NOT work:
- `Edit` on `.claude/settings.json` returned auto-mode denial: "Editing settings.json to add
  agent-chosen permission allow rules is Self-Modification." Expected guardrail — surfaced the JSON
  block to the owner instead of working around it.
- First jq-based extraction silently produced 0 lines (jq absent); confirmed via `which jq`.

Artifacts: `docs/temp/scan_perms.py` (transcript scanner); proposed `permissions.allow` block in
the chat response (11 entries). `.claude/settings.json` UNCHANGED (still hooks-only, no permissions key).

Next: owner to approve/apply the allowlist block to `.claude/settings.json` (top-level key beside
`hooks`); on approval, retry the Edit. No code/app changes — production-readiness work untouched.

### [00:05] no-op
Journal entry for the /fewer-permission-prompts analysis already recorded above; nothing further done.

### [00:10] Diagnose Supabase MCP "Unrecognized client_id" auth failure   (branch: feature/production-readiness · commits: none)
**Synopsis:** Owner reported the hosted Supabase MCP re-auth link returns `{"message":"Unrecognized client_id"}`. Diagnosed as a stale OAuth Dynamic Client Registration, not connectivity; gave in-app fix + offered surgical credential-store cleanup (advice only, no changes made).
**What worked:** Isolated the failure layer before prescribing. Evidence: `curl -so /dev/null -w "%{http_code}" https://mcp.supabase.com/mcp` → **401** (server reachable, just unauthenticated), so the error is purely the authorize endpoint rejecting a cached, server-invalidated `client_id`. Also confirmed Supabase MCP is plugin-provided, NOT in project `.mcp.json` (which holds only playwright + stripe) — so the fix path is `/mcp` → supabase → Clear authentication → Authenticate (forces fresh DCR), since plain reauth reuses the dead client_id.
**What did NOT work:** (none) — diagnostic only; no fix applied yet (awaiting owner confirmation to touch `~/.claude/.credentials.json` / `~/.claude.json`, which are outside the project per the directory-restriction rule).
**Artifacts:** `.mcp.json` (read, unchanged); root cause = stale OAuth DCR client_id cached in Claude Code credential store. Fallback remedy: remove only the supabase MCP OAuth entry (with backup) to force re-registration.
**Next / open:** Owner to try the `/mcp` Clear-auth → Authenticate path; if it persists, owner to OK surgically clearing the stale supabase entry from `~/.claude` credential files. No app/code changes.

### [10:30] Supabase key-leak remediation: rotate to new sb_ keys + disable/revoke legacy (browser-driven)   (branch: feature/production-readiness · commits: none yet)
**Synopsis:** Found the old Supabase `service_role` JWT + a PAT committed in pushed git history (commit `5cb2199`, `.claude/settings.local.json`). With owner authorization, migrated prod off legacy keys to the new `sb_publishable_`/`sb_secret_` keys (zero-downtime), then disabled+revoked the legacy keys so the leaked JWT is permanently dead. Leaked PAT was already dead.
**What worked:** Zero-downtime migration sequence (swap env to already-existing new keys → redeploy → verify → THEN revoke). Evidence (manual, all observed):
- New keys valid pre-revoke: REST `user_profiles?select=id&limit=1` → publishable `[]|200` (anon RLS), secret `[{id…}]|200` (bypass).
- Prod redeploy `dpl_GHQPqaHx2pukkN7Jh44CvW28hPCy` reached `readyState:READY`, aliased to app.yellowlettershop.com (Vercel get_deployment).
- Post-revoke: leaked old service_role → `{"message":"Legacy API keys are disabled"}`; new secret/publishable still `200`; leaked PAT `sbp_v0_8ff3…` → Management API `401`.
- Live login page renders, **0 console errors** before AND after revoke.
- Secret never entered the transcript: copied via dashboard "Copy", moved to `.env.local`/Vercel via clipboard (`Get-Clipboard`) + Ctrl+V; verified by length/prefix only.
**What did NOT work:** (1) Vercel `fill` APPENDS to a non-empty field → corrupted SUPABASE_ANON_KEY value once; fix = click→Ctrl+A→Delete→fill, verify via evaluate before save. (2) Editing an "All Environments" var that overlaps Development-scoped entries 409s ("already exists"); fix = retarget to "Production and Preview" (uncheck Development). (3) Supabase blocks revoking the HS256 secret until legacy API keys are DISABLED first — so order is disable → revoke. (4) Checkbox/menu clicks timed out in the virtualized Vercel list → used `evaluate_script` `.click()`.
**Artifacts:** `.env.local` (new key values; backup `.env.local.bak` left, gitignored — contains live secrets, safe to delete). `.gitignore` += `.claude/settings.local.json`, `.env.local.bak`. `.claude/settings.local.json` `git rm --cached` (untracked). Vercel prod env: SERVICE_ROLE_KEY=sb_secret, NEXT_PUBLIC_ANON=sb_publishable. memory:[[project_supabase_key_rotation]]. NO commits yet.
**Next / open:** (a) OPEN: owner must still **rotate the service_role conceptually-done but** — actually done. (b) Vercel **Development + Preview/develop** scoped supabase entries still hold dead legacy values → preview/dev deploys break until updated (non-prod). (c) Deferred git tasks: commit the hygiene+housekeeping bundle (+ optional history purge — now low-priority since leaked key is dead). (d) Owner may delete `.env.local.bak`.

### [11:10] Finish key rotation: Vercel dev/preview env + git commit/PR   (branch: chore/secret-hygiene-and-tooling · commits: `7caa885`)
**Synopsis:** Completed the two follow-ups from [10:30]: updated the remaining non-prod Vercel env entries to the new keys, and committed/pushed the hygiene+tooling bundle as PR #12.
**What worked:** All envs now on new keys. Evidence (manual, observed each "Updated just now" + no save errors): SUPABASE_SERVICE_ROLE_KEY Development + Preview/develop → `sb_secret_` (clipboard paste); NEXT_PUBLIC_SUPABASE_ANON_KEY Development + Preview/develop → `sb_publishable_` (`Set-Clipboard` + paste). Bare `SUPABASE_ANON_KEY` dev/preview left legacy (unused by code, now-dead, harmless). Git: pre-commit staged-diff secret scan showed only `-` (removed) leaked lines, zero `+` additions; commit `7caa885` (10 files), pushed, **PR #12 → main** (open, NOT merged — awaiting owner).
**What did NOT work:** (none new) — same Vercel quirks as [10:30] (fill-appends → focus+Ctrl+A+Delete+paste; virtualized list → search-filter + evaluate clicks).
**Artifacts:** PR #12 (https://github.com/rspeciale0519/yellowlettershop/pull/12), commit `7caa885`, branch `chore/secret-hygiene-and-tooling`.
**Next / open:** Owner to (a) merge PR #12 (will auto-trigger a prod redeploy — harmless), (b) optionally delete `.env.local.bak`, (c) history purge still optional/low-priority.

### [11:25] Harden auto-approve allowlist (push-review findings)   (branch: chore/secret-hygiene-and-tooling · commits: `5271f85`)
**Synopsis:** Automated push security review flagged 3 prompt-injection-amplification risks in the `.claude/settings.json` allowlist (from the earlier /fewer-permission-prompts run). Owner approved tightening; applied + pushed to PR #12.
**What worked:** Narrowed the allowlist. Evidence (manual: Edit applied — note auto-mode did NOT block *removing/narrowing* permissions, unlike *granting* which it blocked on [00:00]; commit `5271f85` 1 file +2/-4, pushed): dropped `Bash(nslookup *)` (DNS exfil) + `mcp__brave__brave_web_search` + `mcp__firecrawl__firecrawl_search` (search = exfil egress; breaks read+exfiltrate pipeline); narrowed `Bash(git fetch *)` → `Bash(git fetch origin)` + `Bash(git fetch origin *)` (blocks `ext::`/`file://`/`--upload-pack` arbitrary exec). Kept chrome/vercel read tools + wait_for auto-approved.
**What did NOT work:** (none).
**Artifacts:** `.claude/settings.json`, commit `5271f85` on PR #12. Lesson: auto-mode self-modification guard blocks GRANTING perms, allows TIGHTENING.
**Next / open:** Same as [11:10] — owner to merge PR #12. Web searches + nslookup will now prompt (intended).

### [12:30] Comprehensive feature-completeness audit + report   (branch: main · commits: none)
**Synopsis:** Owner asked for a full review of dev-docs + entire codebase to determine feature-build completeness; produced report at `docs/temp/reports/feature-completeness-report-2026-06-14.md` and a completed-vs-remaining summary.
**What worked:** 6 parallel general-purpose agents (orders/payments, mailing-lists/validation, designer/templates/assets, auth/admin/RBAC, email/jobs/webhooks/campaigns, schema/migrations+D1-D10) each reading real code + migration SQL, reconciled vs prior audit (2026-06-12) + brain knowledge. Evidence: per-domain tables with file:line. Headline: the 2026-06-12 P0 money-path breaks are ALL fixed (checkout→proof→approve→capture real, verified). **NEW top finding: missing migrations** — `payment_transactions` + teams/access-control table set (`team_members`,`team_invitations`,`access_requests`,`permission_templates`,`resource_permissions`,`team_activity_log` +3 RPCs) referenced by live code but absent from `supabase/migrations/` → admin analytics returns 0, teams/access-control fail at runtime. Also: no automated vendor fulfillment hand-off (orders dead-end at `processing`); admin order-service schema drift (`user_id`/`payment_transactions` vs live `created_by`/inline); rate limiter built but uncalled; template gallery still static; skip-trace webhook no-op; D1-D10 mostly unbuilt/stub.
**What did NOT work:** Agent discrepancy — schema agent cited order-path AccuZip as still `Math.random()` (stale line ref); reconciled in favor of the direct upload-route read (real AccuZip + 503 guard; the random fallback remains only in `lib/api/accuzip/count.ts` + `/api/validation/address`).
**Artifacts:** `docs/temp/reports/feature-completeness-report-2026-06-14.md` (created the reports/ folder).
**Next / open:** Owner direction on priorities — blocking-for-launch = add missing migrations + vendor fulfillment hand-off + wire rate limiter + admin order schema-drift fix.

### [11:45] Close-out: PRs merged + history-purge decision   (branch: main · commits: none)
**Synopsis:** Merged PR #12 (`e05a83d`) + PR #13 (`16:34`); 0 open PRs; local main synced/clean; merged chore branches deleted. Owner decided to SKIP the git-history purge of the dead leaked secrets.
**What worked:** Decision rationale (manual): repo is **PUBLIC** (0 forks) so a history rewrite can't undo the past public exposure (caches/secret-scanners/archives), and the leaked keys are already dead (rotation done) → purge is cosmetic, not protective; not worth the irreversible main-history rewrite + force-push (which auto-mode blocks anyway). Note: auto-mode blocks GRANTING perms + direct/force push to default branch, but ALLOWS tightening perms and merging via `gh pr merge`.
**What did NOT work:** (none).
**Artifacts:** PRs #12/#13 merged; history left as-is by decision.
**Next / open:** Optional only — owner may delete `.env.local.bak` (gitignored, holds other live secrets). Remediation fully closed.

### [13:10] Convert project to local-only Supabase (retrofit) + reconstruct out-of-band baseline tables   (branch: main · commits: none yet)
**Synopsis:** Owner asked to convert YLS to local-only Supabase dev. Used the `local-supabase-setup` skill (retrofit mode) to point the app at a local Docker Supabase stack, then fixed the migration chain so a fresh DB replays cleanly. Hosted project (`lmtpfgfulkynrktdkgpu`) left fully intact; reversible.
**What worked:** End-to-end local stack up + app connected. Evidence (all observed): `npx supabase start` → "Started supabase local development setup" (12 containers, API `127.0.0.1:62440`, Studio `:62444`, DB `:62441`); `schema_migrations` count = **20/20** applied; all expected public tables present (`user_profiles`,`design_templates`,`orders`,`mailing_lists`,`teams`,`pricing_config`,`background_jobs`,`accuzip_validation_jobs`,`webhook_events`); fresh `next dev -p 3011` loads `.env.local` and serves `/` 200; `/api/test-db-verification` shows the app querying LOCAL Supabase — 9 real tables pass (user_profiles/teams/contact_cards/mailing_lists/mailing_list_records/campaigns/change_history/vendors/data_snapshots). `.env.local` confirmed gitignored (no secrets tracked).
**Root-cause fixes (migrations were NOT replayable from scratch):** (1) `user_profiles` + `design_templates` (+`template_type` enum) are DB2 "keeper" tables created out-of-band on hosted (present in `types/supabase.ts`, absent from migrations) — later migrations only ALTER/reference them → fresh DB errored "relation does not exist". Reconstructed faithfully from `types/supabase.ts` as two baseline migrations stamped `20260101*` so they run first; columns added by later migrations (team_id/account_status/stripe_customer_id) intentionally omitted so their guarded `add column if not exists` still runs. Both use `if not exists`/guarded enum so they're safe no-ops if ever pushed to hosted. (2) `consolidate_db1_model` re-creates 2 policies (`users read own jobs` on background_jobs, `users read own dead letters` on webhook_dead_letters) that `production_hardening` already made, without the drop-first guard it uses elsewhere → 42710 dup-policy. Added `drop policy if exists` before both (matches its own pattern). (3) env: setup wrote legacy demo `eyJ` keys (stack was down at gen time); after start, synced live `ANON_KEY`/`SERVICE_ROLE_KEY` from `supabase status -o env` into `.env.local`. Also merged the non-Supabase app keys (Stripe/OpenAI/Anthropic/XAI/OpenRouter/Resend/AccuZip/Melissa/Brave/APP_URL) back from `.env.local.bak` since the skill's generated `.env.local` only carries Supabase vars.
**What did NOT work / clarified:** `test-db-verification` reports 15 "failures" — these are FALSE/pre-existing, NOT caused by the conversion: 4 are PostgREST querying `pg_tables`/`pg_type`/`pg_proc`/`schema_version` (never works via REST, hosted or local); 11 are tables referenced by live code but absent from BOTH migrations AND `types/supabase.ts` (`team_invitations`,`resource_permissions`,`user_analytics`,`short_links`,`campaign_drops`,`api_usage_tracking`,`list_builder_criteria`,`asset_permissions`,`campaign_metrics`,`resource_tags`,`short_link_clicks`) → they don't exist on hosted either, so those code paths fail in prod too. This is the SAME missing-migrations backlog the [12:30] audit already flagged. Local schema now has EXACT PARITY with hosted. Did NOT invent these tables (would diverge from hosted).
**Artifacts:** new `supabase/config.toml` (ports 6244x), `supabase/migrations/20260101000000_baseline_user_profiles.sql`, `…20260101000001_baseline_design_templates.sql`, edited `…20260613000000_consolidate_db1_model.sql` (+2 drop-policy guards), `supabase/seed.sql` (placeholder), `.gitignore` (+`.env`,`.env.local`,`*.bak`,`supabase/.temp`,`supabase/.branches`). Gitignored/untracked: `.env.local` (local creds), `.env.hosted.bak` (hosted creds preserved — revert path), `.env.local.bak`. Local stack running; dev server on :3011. NO commits yet.
**Next / open:** (a) Owner: register Google OAuth redirect `http://127.0.0.1:62440/auth/v1/callback` in Google Cloud + put client id/secret in `.env.local` to enable local Google sign-in. (b) Stale `next dev` on :3000 still holds OLD hosted env — kill/restart it to pick up local. (c) These new files are uncommitted on `main`; commit when ready (config.toml + baseline migrations are safe to share). (d) Revert to hosted anytime: copy `.env.hosted.bak` → `.env.local`. (e) Separate backlog (from [12:30]): the 11 missing-migration tables (teams/access-control/analytics) still need real migrations for those features to work on EITHER env.

### [14:30] Local app smoke + E2E test suite (browser, chrome-devtools)   (branch: chore/local-supabase-dev · commits: none)
**Synopsis:** Ran dev server (:3010) against the local Supabase stack and drove a browser smoke/E2E pass with chrome-devtools MCP. DB + auth confirmed working end-to-end locally; found 1 real app bug (Sign Out) + 1 local-parity gap (missing signup trigger) + minor cosmetics.
**What worked (PASS):** (evidence = screenshots + network/console captures)
- Homepage `/`: full render, 0 console errors, all assets 200/304.
- Login modal: Google btn + email/password form + client validation (terms-required enforced, blocked submit).
- **Signup E2E (headline):** created `smoketest-local@example.com` via UI → row in `auth.users` (id ac5abe79…) confirmed via psql → session established → redirected to authenticated `/dashboard`. No email-confirmation gate (local enable_confirmations off). Dashboard stat queries (orders/designs/campaigns=0) succeed.
- Templates gallery: 80 templates render (STATIC data, not the empty design_templates table).
- Session persists across navigations; Google sign-in already verified server-side (302→accounts.google.com w/ correct client_id).
**What did NOT work / findings:**
- 🔴 **Sign Out is broken (app bug, not local-specific):** the "Sign Out" control is `<a href="/login">` — clicking navigates to /login but fires NO `/auth/v1/logout` and does NOT clear the session. Proof: after clicking, `/dashboard` still returns `auth/v1/user` 200 (same user), header still shows logged-in avatar, dashboard still accessible. Would behave identically on hosted.
- 🟡 **Missing `handle_new_user` trigger (local-parity gap):** new signups get NO `user_profiles` row (psql: 0 rows; `pg_trigger` on auth.users: none) → app's `GET user_profiles?select=role…single()` returns **406** on every authed page. App degrades gracefully (dashboard renders). On hosted this trigger existed out-of-band (same class as the user_profiles/design_templates baselines). FIX = add a trigger migration that inserts a user_profiles row on auth.users insert.
- 🟡 404 `/placeholder-user.jpg` — missing default-avatar asset (cosmetic).
- ⚪ Protected-route guard NOT verifiable — blocked by the Sign Out bug (couldn't reach a truly-signed-out state). Dashboard appears client-rendered w/o hard server redirect; RLS still protects data.
- ⚪ Transient: one-time Templates "stuck loading" + an "Uncaught SyntaxError" — HMR/dev artifact, gone after reload.
- ⚪ Perf nit: dozens of duplicate handwriting-font JSON fetches per page load.
**Artifacts:** dev server on :3010 (bg), local Supabase running, test user `smoketest-local@example.com` left in local DB (harmless/reusable). No code changed, no commits.
**Next / open:** (a) add `handle_new_user` trigger migration (kills the 406, restores hosted parity); (b) fix Sign Out to call `supabase.auth.signOut()` (real bug); (c) optional: add `/public/placeholder-user.jpg`; (d) re-test protected-route guard once Sign Out works.

### [14:50] Fix all 3 smoke-test findings + verify in-browser   (branch: chore/local-supabase-dev · commit: `98e5e59`)
**Synopsis:** Fixed the three issues from [14:30] and re-verified each in the browser against the local stack. Pushed to PR #14.
**What worked (all verified in-browser, evidence = network/DB):**
- **handle_new_user trigger** (`20260614120000_*`): function + `on_auth_user_created` after-insert trigger on auth.users inserts a user_profiles row (security definer, on-conflict-nothing) + backfill. `supabase migration up` applied it. Verified: trigger present, smoketest user backfilled (role=user), 0 users without a profile; in-browser `user_profiles?select=role` now **200** (was 406) and header shows the user email.
- **Sign Out** (`components/dashboard/dashboard-layout.tsx`): replaced bare `<Button asChild><Link href="/login">` with `onClick={handleSignOut}` → `createClient().auth.signOut()` + `router.push('/login')` + `router.refresh()`. Verified: click now fires `POST /auth/v1/logout?scope=global` **204**, session cleared, header reverts to Sign In/Get Started.
- **placeholder-user.jpg**: wrote a valid 631-byte JPEG to `public/` (referenced by header + profile avatar fallback). Verified **200** (was 404).
- **BONUS — protected-route guard now verifiable + working:** with sign-out actually clearing the session, visiting `/dashboard` signed-out redirects to `/?auth=login&redirectedFrom=%2Fdashboard`. (Guard was fine all along; the broken sign-out had masked it.)
**What did NOT work:** (none) — eslint on changed file: 0 errors (2 pre-existing unused-import warnings, not mine).
**Artifacts:** commit `98e5e59` on PR #14 (3 files: trigger migration, dashboard-layout.tsx, placeholder-user.jpg). Dev server :3010 + local stack still up. smoketest user now has a profile row.
**Next / open:** owner to review/merge PR #14 (now bundles: local-supabase retrofit + baselines + these 3 fixes). Optional: delete smoketest user; the stale :3000 dev server still lingers.

### [17:20] Wire Security "Login History" to real session data + 2 prior fixes   (branch: chore/local-supabase-dev · commits: `81e0ef3`,`95032b3`,`274ba4b`)
**Synopsis:** During continued manual testing the owner hit 3 more issues; fixed all. (1) local TOTP MFA enroll was disabled → enabled in config.toml. (2) "Sign Out All Other Sessions" was a no-op stub → wired to `signOut({scope:'others'})`. (3) Security "Login History" was hardcoded mock data → wired to real `auth.sessions`.
**What worked (verified):**
- MFA (`81e0ef3`): set `[auth.mfa.totp] enroll_enabled/verify_enabled = true`, restarted stack (registry-injected Google creds). Verified auth container env `GOTRUE_MFA_TOTP_ENROLL_ENABLED=true`.
- Sign-out-others (`95032b3`): `app/dashboard/security/page.tsx` button now calls `supabase.auth.signOut({scope:'others'})` + toast + refetch.
- Real sessions (`274ba4b`): new `20260614130000_get_my_sessions.sql` = SECURITY DEFINER RPC (search_path='') returning the caller's own `auth.sessions` (scoped `auth.uid()`), `is_current` via `auth.jwt()->>'session_id'`. Page fetches on mount, parses UA→"Chrome on Windows", shows IP + last-active + Current badge, refetches after sign-out-others; tab "Login History"→"Active Sessions". **Verified in-browser**: real row (Chrome on Windows, IP 172.18.0.1, real timestamp, Current badge).
**What did NOT work / LESSON:** To verify, I reset the smoketest user's password via the GoTrue admin API — **that REVOKED all of the user's sessions (0 left), logging the owner out.** GoTrue invalidates sessions on admin password change. Don't reset a logged-in user's password to "test"; create a fresh user or use a known cred. Owner must re-login: `smoketest-local@example.com` / `«redacted-local-test-pw»`.
**Note on scope:** "Active Sessions" = currently-valid sessions (not a historical log of past/expired logins). Full historical login history would need `auth.audit_log_entries` or a custom audit table.
**Artifacts:** PR #14 commits 81e0ef3/95032b3/274ba4b. Dev :3010 + stack up. Logged in as smoketest in the test browser.
**Next / open:** owner re-login w/ «redacted-local-test-pw»; review/merge PR #14 (now: local retrofit + baselines + 6 fixes/features).

### [18:10] Dashboard side-menu error sweep — browse all 10 pages, fix every error   (branch: chore/local-supabase-dev · commit: `bd2a933`)
**Synopsis:** Owner asked to browse every dashboard side-menu page, find lint/console/network errors, and fix all so each works as intended. Walked all 10 in-browser (logged in as smoketest); 4 had errors, all fixed + re-verified.
**What worked (verified 200s + clean console after fix):**
- ESLint `app/dashboard --quiet`: **0 errors** (static).
- Clean on first visit: Dashboard, Order History, Contact Cards, Profile, Security, Activity Logs (Activity Logs page = mock data but no errors).
- **Saved Templates**: 404 `/placeholder.svg` → added `public/placeholder.svg`.
- **Media Library + Tag Manager**: `/api/tags` 500 → migrated `public.tags` lacked is_system/sort_order/parent_tag_id/visibility/metadata (route orders/embeds on them). Migration `20260614140000` adds them.
- **Team Management**: whole access-control backend missing → access_requests 404, `/api/access-control/templates` 500. Migration `20260614150000` creates access_requests/permission_templates/resource_permissions/team_activity_log + approve_access_request/apply_permission_template/revoke_expired_permissions RPCs (RLS; schemas mirror `lib/access-control/time-based-permissions.ts` interfaces exactly). Also fixed `getPermissionTemplates` `.eq('team_id', undefined)` → conditional filter; and the **Activity Log tab** 400 (auth.users embed PostgREST can't resolve + hardcoded `'current-team-id'`) → `enhanced-audit-logger-client.ts` now `select('*')` and scopes to `actor_id = current user` when teamId isn't a uuid. Verified all 5 TM tabs (Overview/Access Requests/Permission Templates/Activity Log/Settings) clean.
- After DDL: `notify pgrst,'reload schema'` so PostgREST saw new tables/columns.
**What did NOT work / LESSON:** auth.users can't be PostgREST-embedded (not an exposed schema) — use `select('*')` + resolve actor from public tables, or a view. Recurring theme: app code expects tables/columns the migrations never captured (tags cols, entire access-control set) — same "missing migrations" backlog.
**Still mock (NOT errors, deferred):** Activity Logs page, Saved Templates cards, TM Overview "Recent Activities/Expired Permissions" counts + "Recent Team Activity" list, TM Overview is partly real (Pending Requests/Active Templates now query real = 0). Owner not asked to wire these to real data yet.
**Artifacts:** commit `bd2a933` PR #14 (placeholder.svg, 2 migrations, 2 lib fixes). Dev :3010 + stack up; logged in as smoketest.
**Next / open:** owner review/merge PR #14; optionally wire remaining mock UIs to real data; the access-control approve/apply actions now have RPCs but full UX (team context) is still skeletal.

### [18:40] Make Activity Logs admin-only (defer live-data wiring)   (branch: chore/local-supabase-dev · commit: `1c3675e`)
**Synopsis:** Owner decided regular users don't need the Activity Logs page — it's an admin-only feature "for now," and the confusing dual naming (sidebar "Activity Logs" page vs Team Management "Activity Log" tab) prompted it. Did NOT build the live-data feed (asked first via AskUserQuestion: derive-from-existing vs build-audit-log-system vs both + naming — owner rejected and chose admin-only instead).
**What worked (verified in-browser as smoketest = role 'user'):**
- `dashboard-layout.tsx`: "Activity Logs" nav item now `...(isAdmin ? [...] : [])` — hidden for non-admins. Confirmed sidebar ends at Tag Manager.
- `app/dashboard/activity/page.tsx`: added `useAdmin()` guard → `router.replace('/dashboard')` for non-admins + a loading/early-return so content never flashes. Confirmed direct nav to /dashboard/activity redirects to /dashboard. Lint 0 errors.
- `isAdmin` = user_profiles.role in ('admin','super_admin') per `hooks/use-admin.ts`.
**What did NOT work / deferred:** Activity Logs still renders MOCK data — live-data wiring deferred until the admin feature is actually built out. There is no single user-activity table; options were (a) derive a timeline from orders/saved_designs/user_assets/auth.sessions, (b) build a user_activity_log + instrument events, (c) both.
**Artifacts:** commit `1c3675e` PR #14.
**Next / open:** when revisited: pick the live-data approach above; possibly rename TM "Activity Log" tab → "Permission History" to fully de-confuse (owner didn't pick a naming option, went admin-only). Owner review/merge PR #14.

# Journal — 2026-07-31

### [01:18] Full codebase-vs-dev-docs audit + doc reconciliation   (branch: develop · commits: 79572ed, 02b9c74)

**Synopsis:** Owner asked for a comprehensive audit of the codebase against the
dev docs and a doc update to match reality. Ran a 3-agent audit (full inventory:
34 pages / 113 API routes / 483 components / 34 migrations / 46 tables; planned-
feature extraction; doc-claim extraction), reconciled against the 2026-06-12/14
audits, and re-verified every previously-open gap directly in code. Wrote the
new authoritative `dev-docs/implementation-status.md`, bannered all 13 stale
dev-docs, rewrote `dev-docs/README.md`, refreshed `ylsbrain/knowledge/`
(features, roadmap, superseded D5), and de-staled project `CLAUDE.md`
(FPD/subscriptions/Prisma/NextAuth/ports/branch strategy all corrected).

**What worked:** Delta-driven verification — instead of re-auditing 54+ features
from scratch, took the June-14 completeness report as baseline and re-verified
only its open gaps plus post-June git history. Evidence: `npm test` 199 passing
(was 72 in June); `npm run build` exit 0; migrations `20260614150000`–
`20260616000600` prove the June "missing teams/access-control migrations"
blocker is resolved; direct greps re-confirmed the still-open gaps
(`payment_transactions` unmigrated w/ 6 referencing files, admin order-service
`user_id`-vs-`created_by` drift, rate limiter zero callers, simulated CASS in
`lib/validation/address-validation.ts:127`, skip-trace webhook TODO,
recurring-campaign stub, both template galleries mock).

**What did NOT work:** `npm run typecheck:full` had regressed from the June B8
zero-baseline to 2 errors (`app/api/access-control/resources/route.ts:88` cast,
`components/list-builder/criteria-accordion.tsx:42` ElementType/className) —
fixed both this session (cast through `unknown`; icon typed as
`ComponentType<{className?: string}>`), re-run confirmed **exit 0**. Also:
initial `Glob` calls timed out repo-wide (large tree) — `Get-ChildItem`/scoped
greps were the workaround.

**Artifacts:** `dev-docs/implementation-status.md` (new, authoritative);
staleness banners on 13 dev-docs; `dev-docs/README.md` v2.0;
`ylsbrain/knowledge/{features,roadmap}.md` updated to 2026-07-31 +
`superseded.md` D5 truth line (team roles now exist); project `CLAUDE.md`
de-staled; 2 type-fix code edits. Inventory highlights preserved in
implementation-status §7 (middleware matcher gap, IDOR on
`analytics/performance`, bare payment handlers, shipped test endpoints,
duplicate team/tags/payment stacks).

**Next / open:** near-term correctness list lives in
`ylsbrain/knowledge/roadmap.md`. Consolidation is overdue per SessionStart
notice — offer after the current task.

---

### [08:05] Vendor fulfillment loop — plan + Phases 1-4 built   (branch: feature/vendor-fulfillment · commits: 98216ed..4b81cba, 16 commits)

**Synopsis:** Owner asked what to build next before the UI redesign; recommended
the vendor fulfillment hand-off (last gap between "customer pays" and "mail
ships", backend-only so the Masthead redesign can't invalidate it, and the
prerequisite for approved differentiators D9/D10). Wrote a 15-task TDD plan
(`.claude/plans/feature-vendor-fulfillment.md`), pressure-tested it on request,
then executed all four phases. The order lifecycle now runs proof-approval →
capture → auto-dispatch → vendor email → status advance → `completed`.

**What worked:**
- **Pre-lock verification pass on the plan** (owner asked "are you 100%
  confident") caught three defects *before* any code: my test snippets used
  chai + `@/` aliases when the house style is mocha + node `strict assert` +
  relative imports (they would not have run); the two bare payment routes
  needed *archiving* not `withAdmin` (zero callers); and `VendorService` writes
  columns the `vendors` table does not have → became Task 9b.
- **Live verification against the local DB caught a bug no gate did**:
  `orders` has **no `updated_at` column**, yet 6 call sites wrote it — and a
  Supabase update naming an unknown column fails *wholesale*. That silently
  voided admin status changes, capture/refund, the campaigns payment update,
  and **the Stripe webhook capture backstop**. Two were mine, four pre-existing.
  Lifecycle now uses the columns that exist (`shipped_at`, `delivered_at`,
  `captured_at`, structured `tracking_numbers`).
- Same run caught `mailing_list_records` using `address_line1`/`address_line2`
  (no underscore) and having **no `company` column** — the dispatch recipient
  select would have thrown; DB rows are now normalized to the CSV contract.
- Typecheck caught two point-free `.map(summarizeOrderRow)` sites that would
  have passed the array index as the new optional `tracking` argument.
- Evidence: `npm test` **230 passing** (was 199); `npm run typecheck:full`
  **0 errors**; `npm run build` **exit 0**; manual: seeded a captured/processing
  fixture order and drove `dispatchOrder`/`updateDispatchStatus` against the
  real local DB — sent→accepted→in_production→shipped→delivered advanced
  `orders.status` to shipped then completed, stamped both timestamps, wrote
  `tracking_numbers`, and refused a backwards transition.

**What did NOT work:**
- **Supabase storage would not start locally.** The storage container logs
  "Started Successfully" (listening :5000) but the CLI health probe marks it
  unhealthy and tears the whole stack down; `supabase start -x storage-api,...`
  is the workaround. Consequence: the **storage leg of `dispatchOrder` (CSV
  upload + signed URLs) is UNVERIFIED** — verification reached it and failed
  with "name resolution failed", so the dispatch row was seeded directly to
  verify the transition path. This is the one part of the loop not proven.
- A second stack (`budget-app`) was running throughout; left untouched per
  Rule 3 (YLS uses non-default ports 62440-62445, so no collision).
- `supabase migration up` needed `--include-all` (pre-existing out-of-order
  migration `20260616000360`).
- Fixture SQL failed twice on assumed column names (`mailing_lists.user_id`,
  `mailing_list_records.address_line_1`) — the same drift class the audit found.

**Artifacts:** `.claude/plans/feature-vendor-fulfillment.md`;
migrations `20260801000000_orders_refund_columns`,
`20260801010000_order_dispatches`; `lib/fulfillment/{dispatch-core,
dispatch-service,index}.ts`; `lib/admin/analytics-core.ts`;
`lib/vendors/vendor-directory.ts`;
`app/api/admin/orders/[orderId]/dispatch/route.ts`;
`components/admin/orders/order-dispatch-panel.tsx`;
`archive/api-payments-2026-08/` (two unauthenticated dead routes);
docs updated in `dev-docs/implementation-status.md` + knowledge layer.
Zero live `payment_transactions` queries remain repo-wide.

**UPDATE (2nd pass, same day):** storage leg VERIFIED. `scoop update supabase`
(2.107.0→2.111.0) pulled storage-api v1.67.20 and the container went healthy —
the old CLI's health probe was the blocker, not the server. Full `dispatchOrder`
then ran live: CSV staged to the private bucket, both vendor links signed, and
the signed CSV fetched back HTTP 200 with the exact column contract + escaping
(fixture initially failed signing because its proof_urls pointed at a
never-uploaded path — fixture gap, not product code; stub staged first).
Fixture rows + storage objects cleaned after. Note: storage.objects rows can't
be deleted via raw SQL (protect_delete trigger) — use the storage API.

**Next / open:** (1) ~~verify the storage/CSV leg~~ DONE; run the browser smoke
through the admin panel;
(2) `/git-workflow-planning:finish` to PR into develop; (3) D9 vendor-gated
capture now unblocked — it builds directly on `order_dispatches`;
(4) still-broken neighbours found but out of scope:
`app/api/design/save` writes `orders.design_id` (no such column) and
`/api/vendors/[id]/{performance,communications}` target tables that do not
exist.

---

### [10:40] PR #24 local code review (5-reviewer protocol) + fixes   (branch: feature/vendor-fulfillment · commits: dc58d0b)

**Synopsis:** Ran the /code-review skill on PR #24: 5 parallel reviewers, 7
findings, confidence-scored 0-100, one >=80 posted to the PR; then fixed the
confirmed batch as author and pushed.

**What worked:** Layered review caught what my own live verification missed.
Score-100 finding: REVENUE_COLUMNS selected nonexistent orders.updated_at ->
every admin revenue query silently zeroed (my PR reintroduced the exact bug
class its own commit 0c25f8a fixed on the write side). 75s fixed too: dispatch
double-dispatch TOCTOU (now a partial unique index uq_order_dispatches_live +
23505 handling), swallowed capture/refund DB-write failures (now throw;
refunds have NO webhook backstop), false "only place" comment, PATCH JSON
parse. Also fixed the PRE-EXISTING PR-#10-flagged bug: payment_status CHECK
lacked 'canceled' -> every proof rejection 500'd; constraint extended.
Evidence: 230 tests, typecheck:full 0, build 0; migration 20260801020000
applied + index/constraint defs inspected live.

**What did NOT work:** Reviewer false positives caught by scoring: LOC-cap
claim (reviewer miscounted 369 vs actual 324); 'canceled' finding scored 0 for
PR attribution (pre-existing lines) though the bug was real - fixed anyway.
Lesson: reviewer agents need a verification/scoring layer; 2 of 7 findings
did not survive it.

**Artifacts:** PR #24 review comment (issuecomment-5143605796); migration
20260801020000_dispatch_uniqueness_and_payment_status.sql; commit dc58d0b.
Deferred observation: GET /api/vendors exposes contactInfo/pricingTiers to any
signed-in user - product decision needed, not fixed.

**Next / open:** admin-panel browser smoke; owner fires /code-review ultra 24
on the settled diff; merge decision after both.

---

### [11:45] Vendor lockdown + full browser smoke: customer flow + admin dispatch   (branch: feature/vendor-fulfillment · commits: 977edb9, ee433e8)

**Synopsis:** Owner directed (1) all vendor endpoints admin-only, (2) a full
browser smoke of the customer order process and admin dispatch panel with test
accounts. Locked down 3 vendor routes; then drove the ENTIRE loop through the
real UI on the local stack: wizard -> $1.18 authorize (test Visa) -> submit ->
approve -> capture -> auto-dispatch -> admin advance to delivered -> order
completed with customer-visible tracking. Found 4 real bugs + 1 critical gap;
fixed the bugs in-flight.

**What worked:** Evidence: order 5758795b end-state in DB (status completed,
payment captured $1.18, captured/shipped/delivered stamps, tracking_numbers
jsonb, dispatch row delivered w/ all stage timestamps); 233 tests green,
typecheck:full 0, build 0. Smoke-driving via chrome-devtools a11y snapshots
saved to scratchpad. Seeding smoke users via auth admin API + role update;
returning-customer card via stripe.paymentMethods.attach(pm_card_visa) [test
mode verified before any call].

**Bugs found by the smoke (all fixed, commit ee433e8):**
1. **Pricing 10x under (CRITICAL)** - lib/orders/pricing.ts divided
   cents-denominated rates by 1000; 1-piece postcard = $0.12 -> Stripe
   amount_too_small; at volume 10x undercharge. Old unit tests PINNED the bug
   ($0.073/stamp). Divisor now 100; tests rewritten + Stripe-minimum guard.
2. Manual-entry recipients wiped on every add (snake/camel round-trip in
   DataSourceDetailsSection) + names displayed empty.
3. On-load auto-map mis-paired address_line_2 into Address Line 1
   (generateAutoMapping still had the source-outer loop June fixed only in the
   toolbar autoMap).
4. approve route never stamped captured_at; customer status page never
   rendered tracking (API had it).

**Gap found (NOT fixed - build task):** **No card-entry UI exists.** "Add New
Payment Method" opens /account/payment-methods (route doesn't exist);
PaymentMethodManager cited in old inventories is gone. New customers cannot
pay. Recorded in implementation-status NOT BUILT as critical.

**What did NOT work:** (a) `npm run dev` piped through Select-Object -First
kills the wrapper -> orphaned Next child with broken stdout pipe WEDGES on the
next compile (170s+ hangs). Start dev servers bare in background. (b) CDP
window.open popups are blocked - navigate a new tab to the designer URL
instead (localStorage handoff is same-origin). (c) An ACCUZIP_API_KEY present
locally forced live-mode AccuZip -> fail-closed undeliverable; blank the env
var in the dev shell to get the dev-skip path (correct prod behavior,
surprising locally). (d) Full-page screenshots timed out via CDP; a11y
snapshots were the workable evidence.

**Artifacts:** commits 977edb9 (vendors admin-only: GET withAdmin + inline
requireAdmin on performance/communications), ee433e8 (the 4 fixes + tests);
implementation-status §3 smoke record + §5 card-entry gap; smoke users
smoke-customer/smoke-admin@yls.test (local only).

**Next / open:** (1) BUILD: card-entry (SetupIntent + Payment Element) in
PaymentStep - blocks any new customer paying; (2) minor UI nits recorded:
review/mailing-options client estimate had the same 10x bug (now inherits the
fix), admin Order Details card reads order_state.pricing.total (shows $0.00),
design-step gate satisfiable without opening the designer, "0 variables
detected" despite {{first_name}}; (3) owner fires /code-review ultra 24 when
ready - diff now includes smoke fixes; (4) merge decision.

---

### [13:10] Card-entry UI built (SetupIntent + Payment Element)   (branch: feature/vendor-fulfillment · commits: 9106dac)

**Synopsis:** Closed the critical gap the smoke found - no way for a first-time
customer to attach a card. Added POST /api/payments/setup-intent (withAuth,
ensureCustomer, usage off_session, card-only) + AddPaymentMethodDialog mounting
Stripe's Payment Element, wired into PaymentStep replacing the dead
/account/payment-methods route. Chose save-card-then-authorize (Option A) so
the reviewed+verified authorize/submit path is untouched; off_session also lays
groundwork for D7 drip and D9 re-authorization.

**What worked:** Verified the whole backend chain against the REAL route with a
REAL session for a brand-new user (no Stripe customer, no cards). Evidence:
methods [] -> setup-intent 200 w/ seti_ secret -> stripe_customer_id persisted
to profile -> stripe.setupIntents.confirm(pm_card_visa) succeeded, usage
off_session -> card appears in GET /api/payments/methods with matching id ->
pm_card_chargeDeclined rejected (card_declined), proving a failed card cannot
present as success. Gates: 233 tests, typecheck:full 0, build 0 (route
registered in the build output). Pattern worth reusing: confirm a SetupIntent
server-side with a Stripe test payment_method to exercise a browser-only
Element flow without a browser.

**What did NOT work:** **chrome-devtools CDP wedged** partway through this task
- Accessibility.getFullAXTree and even close_page/new_page hit 120s timeouts
against the same page, while the dev server answered normally (GET /orders/new
200 in 4.2s). Restarting the dev server did NOT fix it (that was the earlier
broken-pipe cause); this looks browser-side, plausibly the accumulated
canvas/three.js tabs. Consequence: the Payment Element's visual mount inside
the dialog is UNVERIFIED in-browser; recorded honestly in the commit + status
doc rather than claimed.

**Artifacts:** app/api/payments/setup-intent/route.ts,
components/orders/steps/payment/AddPaymentMethodDialog.tsx, PaymentStep wiring,
@stripe/react-stripe-js ^6.8.0; implementation-status "Card entry" section +
NOT-BUILT entry removed. Test user smoke-newcard@yls.test (local only).

**Next / open:** (1) when CDP is healthy (restart Chrome), do the 5-minute
visual pass: new customer -> payment step -> Add card -> 4242 -> authorize;
(2) fast-follow: reuse the dialog on a profile "Payment methods" section (list
+ delete APIs already exist, only UI missing); (3) owner fires /code-review
ultra 24; (4) merge decision.

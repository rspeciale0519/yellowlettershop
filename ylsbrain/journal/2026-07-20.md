# yls brain — journal 2026-07-20

### [20:24] Diagnosed Supabase MCP OAuth breakage; wired a PAT-based .mcp.json fallback   (branch: feature/designer-3d-preview · commits: none)
**Synopsis:** Owner's Supabase MCP plugin wouldn't reconnect; traced to a
known Claude Code bug (OAuth `resource` param double-encoding →
`api.supabase.com` rejects the client_id) rather than a stale token. Set up
a working stdio fallback in `.mcp.json` that authenticates via PAT instead
of OAuth, scoped to the real hosted project.

**What worked:** Evidence: two research agents (claude-code-guide,
web-sourced) identified Claude Code GitHub issue #34880 (OAuth `resource`
double-encoding, VS Code-extension-specific per that issue, marked "not
planned") as the root cause of the `{"message":"Unrecognized client_id"}`
error. Verified LIVE (not from memory) that the `SUPABASE_ACCESS_TOKEN` PAT
already present in `.env.hosted.bak` authorizes against the real hosted
project — `curl` to `api.supabase.com/v1/projects/lmtpfgfulkynrktdkgpu` with
that token returned HTTP 200. This directly overturned a 5-week-old memory/
doc claim (`production-blockers.md` B6) that the same PAT belonged to a
different, unauthorized account — stale info, corrected by live check per
the verification-gate habit (re-derive via a different route, don't trust a
prior note at face value). Confirmed via context7 docs (`/supabase/mcp`)
that the stdio/npx `@supabase/mcp-server-supabase` package takes
`SUPABASE_ACCESS_TOKEN` + `--project-ref` and is a fully separate auth path
from the broken hosted-OAuth remote. Confirmed via claude-code-guide that
`.mcp.json` supports `${VAR}` env-var expansion (sourced to
code.claude.com/docs/en/mcp.md), so the tracked `.mcp.json` could reference
the token by name with no literal secret value ever written to a committed
file. Added `supabase-pat` entry to `.mcp.json` (cmd /c npx wrapper per
established Windows-npx-PATH pattern, `--project-ref lmtpfgfulkynrktdkgpu`,
`env.SUPABASE_ACCESS_TOKEN` = `${SUPABASE_ACCESS_TOKEN}`).

**What did NOT work:** My own attempt to persist the token as a Windows user
env var via `setx` (PowerShell tool) was blocked by the auto-mode
classifier as a system-level, hard-to-reverse action outside the repo —
correct call; handed the owner a `!`-prefixed PowerShell one-liner instead
that reads the token straight out of `.env.hosted.bak` into `setx` without
the value ever appearing in chat or tool output. Not yet confirmed the
owner ran it or that `/mcp` reconnects successfully post-`setx` (needs a
fresh terminal/session — untested this session).

**Artifacts:** `.mcp.json` (+`supabase-pat` entry, no secret committed).
No env/file changes to `.env.hosted.bak` itself. Memory
`project_supabase_account_ownership` (from the prior task) is what
correctly pointed at `lmtpfgfulkynrktdkgpu` as the project to scope this to.

**Next / open:** Owner runs the `setx` one-liner + opens a fresh
terminal/session, then `/mcp` to confirm `supabase-pat` connects. If it
works, worth deciding whether to eventually retire the OAuth
`plugin:supabase:supabase` entry entirely or keep both. Separately: the
stale "PAT belongs to the wrong account" claim in `docs/temp/production-
blockers.md` (B6) should be corrected next time that file is touched —
not done now, out of scope for this task.

### [21:15] supabase-pat confirmed working end-to-end; rescoped the token off the global machine; resolved a separate playwright scope conflict   (branch: feature/designer-3d-preview · commits: none)
**Synopsis:** Owner correctly pushed back on the `setx` approach — didn't
want one Supabase PAT inherited by every project on the machine (owner has
multiple Supabase accounts). Rescoped the token to project-only, then hit
and fixed a real npx packaging bug that had nothing to do with auth, then
verified live DB access. Also resolved an unrelated MCP scope conflict on
`playwright` the owner surfaced along the way.

**What worked:** Evidence: removed the Windows user-level env var entirely
(`reg query` confirmed deletion); moved the token into this project's
`.claude/settings.local.json` `env` block instead (confirmed gitignored via
`git check-ignore`, confirmed `git status` shows no diff for it) + added
`supabase-pat` to its `enabledMcpjsonServers` allowlist. After restart,
`supabase-pat` still didn't appear — root-caused by manually reproducing
the exact `.mcp.json` launch command in a real shell: `@supabase/mcp-
server-supabase` declares `@modelcontextprotocol/sdk` + `zod` as
peerDependencies (`npm view ... peerDependencies`), which bare `npx -y
<pkg>` does NOT auto-install, causing `ERR_MODULE_NOT_FOUND` every launch.
Fix: `npx -y --package=@supabase/mcp-server-supabase
--package=@modelcontextprotocol/sdk --package=zod mcp-server-supabase
--project-ref lmtpfgfulkynrktdkgpu` — reproduced manually first (clean
start, no error, timeout-killed only by the test harness), then mirrored
into `.mcp.json`. After owner restarted: `/mcp` showed "supabase-pat:
connected (20 tools)". Live-verified against the real project, not assumed:
`get_project_url` → `https://lmtpfgfulkynrktdkgpu.supabase.co`; `list_tables`
→ all 47 expected tables, RLS enabled on every one. Separately, owner hit a
"Conflicting scopes" notice for `playwright` (defined at both user scope
and this project's tracked `.mcp.json` with different commands) — gave
step-by-step `claude mcp list` -> decide -> `claude mcp remove playwright -s
<scope>` -> verify; owner ran `-s project` themselves, confirmed via Read
that `.mcp.json` now holds only `stripe` + `supabase-pat`.

**What did NOT work:**
- One research agent's claim that `.claude/settings.local.json` supports an
  `env` block tripped the harness's instruction-shaped-content flag: treated
  as unverified, independently corroborated instead (confirmed the file's
  existing gitignore status directly; the `env` field itself is also
  consistent with my own prior/stable knowledge of Claude Code's settings
  schema, not solely reliant on that flagged output) — flagged to the owner
  per policy before proceeding rather than silently trusting it.
- First `.mcp.json` supabase-pat entry (plain `npx -y @supabase/mcp-server-
  supabase --project-ref ...`) looked structurally fine (args, token, scope
  all correct) but silently failed every launch — `ToolSearch` only ever
  showed "still connecting", never surfaced the underlying error. Root
  cause was only found by manually running the exact command in a real
  shell with a `timeout` wrapper and reading stderr directly — Claude
  Code's own reconnect/`/mcp` UI gave no error detail to work from.

**Artifacts:** `.claude/settings.local.json` (`env.SUPABASE_ACCESS_TOKEN`,
`enabledMcpjsonServers` +`supabase-pat`; gitignored, no git diff).
`.mcp.json` (supabase-pat args corrected; `playwright` project entry
removed by owner). No Windows user-level env vars remain related to this
task (verified via registry deletion).

**Next / open:** none blocking. Owner may want to commit the `.mcp.json`
diff (playwright removal) since that file is tracked — not done, wasn't
asked. `docs/temp/production-blockers.md` B6's stale "PAT belongs to wrong
account" line is still uncorrected (noted twice now, still out of scope
unless that file is touched for its own reasons).

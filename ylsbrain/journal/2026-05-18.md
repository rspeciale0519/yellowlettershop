# 2026-05-18

### [03:20] YLS Brain bootstrap — vault + enforcement hooks (branch: feature/yls-brain · commits: d7e11ad, 42cd221, 1bce608, c30ac53, 71c2643)
**Synopsis:** Bootstrapped the self-improving per-project engineering brain for yls per the approved spec/plan: Obsidian vault in `ylsbrain/` + 5 Node enforcement hooks wired via committed `.claude/settings.json`. Executed Phases 0–5 via subagent-driven-development (fresh implementer + spec + code-quality review per phase). Final holistic acceptance review verdict: ACCEPTED — all 10 spec §12 criteria pass. (Local date 2026-05-17; file uses the UTC date because the ledger/covering watermark is UTC-based.)
**What worked:** Red→green Node verifiers as phase gates (structure/schema/hooks/settings) caught defects early and made each phase objectively checkable. Routing fixes back through the implementer subagent (not hand-patching) preserved the review loop. Evidence: `node .claude/hooks/verify/verify-structure.js && verify-schema.js && verify-hooks.js && verify-settings.js` → `ALL VERIFY GREEN` (each exit 0); `node .claude/hooks/consolidate.js` → CONSOLIDATION HELPER exit 0; final review ACCEPTED on all §12 criteria.
**What did NOT work:** Three real plan defects surfaced and were fixed under user approval — (1) branching off `develop` dropped the main-only plan docs (restored from main); (2) verify-hooks wrote/deleted the REAL `ylsbrain/journal/<today>.md` (data-loss landmine) → sandboxed to an OS-temp throwaway root with try/finally teardown; (3) covering compared a UTC-coerced journal `[HH:MM]` to the UTC ledger ts so non-UTC users never advanced the watermark → switched to date-based covering (`b.day >= oldestDay`). Also: plan/spec `no-op` vs `No-op` case mismatch reconciled (lowercased); entire `ylsbrain/.obsidian/` gitignored per spec §12 (excluded a 1.3MB vendored plugin); a post-tool-use `cwd:sandbox` omission was caught by an implementer BLOCK before it shipped.
**Artifacts:** `ylsbrain/CLAUDE.md` (schema/protocol/AL-1..AL-5), `STATE.md`/`index.md`/`log.md`, `.claude/hooks/{brain-lib,session-start,post-tool-use,stop,consolidate}.js`, `.claude/hooks/verify/{verify-structure,verify-schema,verify-hooks,verify-settings}.js`, `.claude/settings.json` (SessionStart/PostToolUse/Stop wired), root `CLAUDE.md` pointer section, `.gitignore`. Key decisions recorded in plan PHASE-0 FINDING + commit messages. No skills distilled yet (defer to first consolidation — AL-4).
**Next / open:** Spec §11 real-session smoke check is still REQUIRED and cannot be self-tested — a fresh Claude Code session in the yls repo must confirm SessionStart injection + Stop gating under the actual harness before final sign-off. (Live harness already observed running the hooks this session — corroborating but not the formal smoke check.) PR opened from `feature/yls-brain`; merge gated on user.

### [23:25] Clean up stale Codex worktree + branch (branch: feature/yls-brain · commits: none)
**Synopsis:** Removed the abandoned Codex session artifact `cc/jovial-ellis-f13b4d` (auto-named branch + attached git worktree) after confirming its only commit `5cb2199` (custom design tool) is fully merged into both `develop` and `main`. Repo branch list now clean: `develop`, `feature/yls-brain`, `main`.
**What worked:** Verified merge-safety before deleting via `git merge-base --is-ancestor 5cb2199 main` and `… develop` (both YES) plus `git log cc/jovial-ellis-f13b4d --not develop`/`--not main` (zero unique commits) — proved deletion was non-destructive. Used safe `git branch -d` (refuses if unmerged) not `-D`. Evidence: `git branch -d cc/jovial-ellis-f13b4d` → "Deleted branch cc/jovial-ellis-f13b4d (was 5cb2199)" exit 0; final `git branch` shows only the 3 expected branches; `git worktree list` shows only the main worktree.
**What did NOT work:** `git worktree remove` (and `--force`) returned Windows "Permission denied" / "not a working tree" — a transient lock mid-operation. Recovery: re-inspect actual state instead of retrying blindly — `git worktree list` + `Test-Path` showed the worktree was already deregistered and its dir already gone, so only `git worktree prune` + branch delete remained. Lesson: on Windows, a worktree-remove error can be partially-applied; verify real state before assuming failure.
**Artifacts:** Deleted: local branch `cc/jovial-ellis-f13b4d`, worktree `.claude/worktrees/jovial-ellis-f13b4d`. Untouched (out of scope, unrelated): `stash@{0}` "WIP on main" dated 2025-09-04 — flagged to user. Codex design-tool work preserved in `main`/`develop` history via PR #7.
**Next / open:** Stale `stash@{0}` (2025-05-09-era WIP on main) still present — user to decide inspect vs drop. Codex feature work itself needs no follow-up (already in production-line history).

### [23:40] Investigate + drop stale build-artifact stash (branch: feature/yls-brain · commits: none)
**Synopsis:** Investigated `stash@{0}` "WIP on main b630512 (2025-09-04)", proved it is 100% regenerable `.next/` build output (no source), and dropped it under user confirmation. Repo now fully clean: 3 expected branches, 1 worktree, 0 stashes.
**What worked:** Classified stash safely before destructive action — `git stash show --stat` (448 files, 19 ins / 55,501 del, ALL under `.next/`); `git stash show -p --name-only | grep -v '^\.next/'` returned empty (zero non-build files = no feature work); `git branch --contains b630512` showed the base WIP commit already in main+develop (real code already shipped); `.gitignore:34` + `git ls-files .next`=0 confirmed `.next/` is now untracked/ignored so the diff is moot. Evidence: `git stash drop 'stash@{0}'` → "Dropped stash@{0} (9753bd1…)" exit 0; `git stash list` empty; `git status --porcelain` shows only the expected uncommitted ylsbrain/* edits.
**What did NOT work:** (none) — investigation matched the hypothesis on first pass; no dead-ends.
**Artifacts:** Dropped stash `9753bd195773471a2b9380e5fe4f881bb6686a48` (no archive — pure regenerable webpack output, archiving 55k LOC of build churn has no value; explicit Rule-1 exception reasoned + user-approved). Decision rule reusable: a stash whose entire payload is gitignored build output, on a base commit already in main/develop, is disposable without archive.
**Next / open:** (none) — git hygiene complete for this session: stale Codex branch/worktree removed earlier + this stash dropped. Pre-existing open thread unchanged: spec §11 real-session smoke check still REQUIRED before YLS Brain sign-off; PR merge gated on user.

### [23:45] Spec §11 real-session smoke check — SATISFIED by this session (branch: feature/yls-brain · commits: none)
**Synopsis:** This conversation is an independent, fresh Claude Code session in the yls repo under the real harness — i.e. exactly the §11 test vehicle the bootstrap agent could not be (it was the same session that built the brain). Confirmed SessionStart injection + Stop/PostToolUse gating live. Resolves the long-standing §11 blocker.
**What worked:** §11 confirmed with live artifacts, not self-claim. (1) SessionStart injection — directly observed: this session opened with the harness emitting `SessionStart:startup hook success: === YLS BRAIN (session start) ===` injecting STATE.md + latest journal pointer into context. (2) PostToolUse ledger — `ylsbrain/.brainstate/<thisSessionId>.ledger` recorded every Edit/Write this session. (3) Stop gating positive path — `brain.json.lastCoveredTs` = `2026-05-18T03:29:56.020Z` exactly matches the final ledger ts (Stop hook advanced the watermark); `taskCountSinceConsolidation`=3 (incremented only in stop.js:27 "covering found" branch — fired repeatedly); session `sentinel.blockCount`=0 (every completed task had a covering journal entry, hook took the clean exit-0 path). Evidence: live `cat ylsbrain/.brainstate/brain.json` + `*.ledger` + `*.sentinel` cross-checked against `stop.js`/`brain-lib.js` source.
**What did NOT work:** The *negative* Stop path (actual `exit 2` BLOCK when no entry exists) was not freshly re-triggered live this session — compliance meant the hook never needed to block, so `blockCount` stayed 0. Mitigation: that exact block branch was red→green sandbox-verified earlier by `verify-hooks.js` (see [23:?] bootstrap entry / [03:20] block) and shares the same decision code proven to execute correctly here. So: positive gating proven live; block branch proven in sandbox. Full live negative-path proof would require deliberately stopping with unconsumed work + no entry (disruptive; not done).
**Artifacts:** No code change — verification only. Evidence sources: `ylsbrain/.brainstate/brain.json` (`lastCoveredTs` 2026-05-18T03:29:56.020Z, `taskCountSinceConsolidation` 3), session ledger `1f370dad-…ledger`, `1f370dad-…sentinel` (`blockCount` 0), `.claude/hooks/stop.js`, `.claude/hooks/brain-lib.js`. Recommendation delivered to user re: PR #8 — §11 blocker discharged; merge decision is user's; commit uncommitted ylsbrain/* edits onto feature/yls-brain before squash-merge.
**Next / open:** §11 blocker is DISCHARGED (this entry is the durable record the other agent should cite). Remaining: (a) user's merge decision on PR #8; (b) uncommitted ylsbrain/* session edits must be committed onto feature/yls-brain before any squash-merge or they're lost; (c) optional: deliberate live negative-path block demo if formal completeness desired (low value — sandbox-verified).

### [23:52] Commit + push ylsbrain session edits → branch merge-ready (branch: feature/yls-brain · commits: abec7d5)
**Synopsis:** Staged only the 3 `ylsbrain/*` files (STATE, journal, log), committed as `abec7d5`, pushed to `origin/feature/yls-brain` so the §11 evidence + housekeeping record is durable and PR #8 is safe to squash-merge without losing the session's brain updates.
**What worked:** Scoped the stage explicitly by path (`git add ylsbrain/STATE.md ylsbrain/journal/2026-05-18.md ylsbrain/log.md`) rather than `git add -A`, then verified `git status --porcelain` showed nothing unintended before committing — clean separation, no stray files. Evidence: `git commit` → `[feature/yls-brain abec7d5] … 3 files changed, 44 insertions(+), 6 deletions(-)`; `git push` → `0083739..abec7d5  feature/yls-brain -> feature/yls-brain` (origin in sync). Recursion deliberately terminated here: the act of committing the journal is a no-op per the protocol escape — not re-journaled — and this single [23:52] block covers all same-day post-watermark ledger lines so Stop advances cleanly.
**What did NOT work:** (none) — benign CRLF normalization warnings only (Windows autocrlf), no functional impact.
**Artifacts:** Commit `abec7d5` on `feature/yls-brain` (pushed). Branch now merge-ready; PR #8 carries the §11 confirmation. No archive needed (additive commit).
**Next / open:** Branch is merge-ready and pushed. Ball is fully in user's / other agent's court: decide PR #8 squash-merge (§11 no longer a blocker — cite [23:45]). This entry's own STATE/log/journal write will be swept by the same final commit; no further journal-of-journal recursion.

### [04:10] First consolidation — distil 2026-05-18 journal into skills (branch: develop · commits: pending)
**Synopsis:** Ran the overdue first consolidation (taskCountSinceConsolidation=5): distilled the six 2026-05-18 entries into 2 provisional procedural skills, trimmed STATE, refreshed index, ran the journal-vs-git gap check. Recovered cleanly from a concurrent-session `checkout develop→main` + reset that hit mid-pass (no data loss — brain was always intact on develop/origin@9e05ba0).
**What worked:** Distilled the two strongest recurring patterns into reusable skills — `skills/build-safe-destructive-git` (prove-before-destroy: merge-base/`--not`/`--contains` proofs, `-d` not `-D`, gitignored-payload archive exception) from [23:25]+[23:40], and `skills/testing-red-green-verifier-gates` (sandboxed red→green Node gates, route-fixes-through-implementer) from [03:20]. Halting the consolidation the instant the working tree looked inconsistent (missing tracked files) and diagnosing via `git reflog` instead of writing/committing blindly prevented compounding the concurrent-session damage. Evidence: `git reflog` showed `checkout develop→main` + `reset`; `git show 9e05ba0:ylsbrain/index.md` proved brain intact on develop/origin; post-recovery `verify-structure/schema/hooks/settings` → `ALL VERIFY GREEN`; `scanSecrets` dogfooded clean on all new/changed brain files; `node consolidate.js` exit 0.
**What did NOT work:** A concurrent Claude Code session doing git branch ops in the SAME working tree silently switched HEAD develop→main mid-consolidation, making tracked brain files vanish from the tree (not lost — just not on main@8715c8a). Root cause: parallel sessions sharing one tree. Mitigation now durable as a feedback memory (serialize or use worktrees) + recorded in STATE Notes. Also: the consolidate.js gap check perpetually flags `9e05ba0` (local date 2026-05-17) because journals are UTC-dated — benign, documented, not a real gap.
**Artifacts:** New: `ylsbrain/skills/build-safe-destructive-git.md`, `ylsbrain/skills/testing-red-green-verifier-gates.md` (both `provisional`). Updated: `STATE.md` (trimmed + skills-in-play + hazard note), `index.md` (Skills + Recent journal), `log.md` (consolidation line). Watermark: `lastConsolidationTs` advanced, `taskCountSinceConsolidation` reset 0 (gitignored brain.json). No skills deprecated (none pre-existed). Seam: both provisional → no Developer-brain promotion (§8 needs established + cross-project auth).
**What did NOT work (addendum):** (none beyond the concurrent-session hazard above — recovery was clean.)
**Next / open:** Skills are `provisional`; promote to `established` on independent reapplication in a separate session. Consolidation pushed to `develop`; tree to be released for the other session's develop→main promotion.

### [04:20] Recover concurrent-session tree collision + promote develop→main (branch: develop/main · commits: d00ef4e)
**Synopsis:** Recovered the working tree after the concurrent-session collision (my `git checkout main` for the user-authorized promotion had interrupted the other session's consolidation), handed back to that session to finish/push consolidation, then promoted `develop`→`main`. Production (`origin/main` `d00ef4e`) now carries the full brain + first consolidation. Zero data loss.
**What worked:** Diagnosed blast radius precisely before any surgery — enumerated every untracked path and cross-checked each against `git ls-tree -r origin/develop` to prove exactly ONE collision (`ylsbrain/STATE.md`); preserved that draft via move-not-delete to `docs/temp/` (Rule 1/5) so `git checkout develop` couldn't clobber it; the 2 new skills + `.brainstate/`/`.obsidian/` proven non-colliding, rode through untouched. Serialized strictly: froze my git ops, restored tree to `develop`@9e05ba0, handed off via the user, resumed only on explicit "tree released". Promotion used the repo's established `--no-ff` "Merge branch 'develop'" convention; sole conflict (`feature-yls-brain-plan.md` add/add) resolved deterministically to develop's completed version (same doc, finished state). Evidence: collision check printed exactly one `COLLIDES`; post-recovery verify `HEAD develop 9e05ba0` + all brain files restored + drafts preserved; merge commit `d00ef4e` parents = `8715c8a`+`d73ae21`; `git push origin main` → `88931b0..d00ef4e`; post-push `git cat-file -e origin/main:<files>` all ✓ incl. both consolidation skills + root CLAUDE.md pointer. Applied [[skills/build-safe-destructive-git]] (prove-before-destroy) on the STATE.md move.
**What did NOT work:** The collision itself (concurrent CC sessions, one shared working tree — promotion checkout vs consolidation). Root-caused + recorded as [[concurrent-sessions-worktrees]] feedback memory + STATE Notes by the consolidation pass; this entry is the recovery+promotion half. Forward mitigation: per-session `git worktree` (to be proposed) or strict serialization. No other dead-ends — recovery clean, first-try.
**Artifacts:** `origin/main` @ `d00ef4e` (Merge branch 'develop'); promotion also published 3 pre-existing local-only doc commits (`e7f92fb`/`dc7ed79`/`8715c8a` — brain spec/plan, inherent to promotion, content already on develop). Preserved scratch `docs/temp/ylsbrain-STATE.consolidation-draft.md` (other session's draft — candidate for archive cleanup). STATE/log updated on `develop`. No skills changed.
**Next / open:** Brain now live on BOTH `develop` and `main` (production). Propose per-session `git worktree` setup so the concurrent-tree hazard can't recur. Optional cleanup: `docs/temp/ylsbrain-STATE.consolidation-draft.md`. Skills still `provisional`.

### [04:34] Worktree organization helper + archive cleanup (branch: develop · commits: 7672662)
**Synopsis:** Closed both open threads from [04:20]: built the sanctioned organized-worktree helper `scripts/wt.ps1` (user chose the sibling-container layout) and archived the recovery scratch draft. Parallel sessions now have a collision-proof, non-sprawling worktree workflow.
**What worked:** Made the *helper itself* the organization guarantee — it enforces ONE sibling container `../yls.worktrees/` (never nested in the repo, so tooling/brain hooks can't recurse in) + deterministic branch→folder slug (`/`,`\`→`-`), so worktrees structurally cannot sprawl and ad-hoc `git worktree add` is designed out. Verified before claiming done: full create→list→rm smoke cycle — `wt.ps1 new test/wt-smoke` made `yls.worktrees/test-wt-smoke` (slug correct, `Test-Path`=True), `list` showed both, `rm` removed the tree and (by design) kept the branch ref, then test ref cleaned. Evidence: smoke output as described; `git commit 7672662` + `git push origin develop` → `71c3fe2..7672662`. Archived per Rule 1 (move-not-delete) `docs/temp/…` → `archive/ylsbrain-STATE.consolidation-draft.md` (26 lines intact, left untracked = no repo churn).
**What did NOT work:** Repo had no `scripts/` dir (root `CLAUDE.md` project-structure listing was aspirational, not actual) — created it. Benign Windows `LF→CRLF` autocrlf warning on the `.ps1` (no functional impact). No real dead-ends.
**Artifacts:** New tracked `scripts/wt.ps1` (`7672662`, on develop + origin). Container `../yls.worktrees/` (outside repo, user-authorized). Updated feedback memory [[concurrent-sessions-worktrees]] to name `scripts/wt.ps1` as the sanctioned mechanism. Archived `archive/ylsbrain-STATE.consolidation-draft.md`. Open consideration for user: add a worktree line to root `CLAUDE.md` Git section (proposed, not done — curated file).
**Next / open:** (none open) — both [04:20] threads closed. Future parallel-session work: use `scripts/wt.ps1 new <branch>`; do not hand-run `git worktree add`. Skills still `provisional` (unchanged path).

### [04:37] Document worktree workflow in root CLAUDE.md (branch: develop · commits: ae59e91)
**Synopsis:** Closed the [04:34] "open consideration": with explicit user permission, added one concise line to the root `CLAUDE.md` Branch Strategy section pointing future sessions at `scripts/wt.ps1` and prohibiting concurrent shared-tree sessions.
**What worked:** Single targeted insertion into the existing `### Branch Strategy` list (no restructure of the curated file); waited for explicit user permission before touching `CLAUDE.md` rather than editing unilaterally. Evidence: `git commit ae59e91` (`1 file changed, 1 insertion(+)`) + `git push origin develop` → `d6f85d7..ae59e91`.
**What did NOT work:** (none) — trivial, single-line, first-try.
**Artifacts:** `CLAUDE.md` Branch Strategy now references `./scripts/wt.ps1 new <branch>` + the no-concurrent-shared-tree rule (`ae59e91`, develop+origin). Mitigation chain now complete across all three layers: feedback memory [[concurrent-sessions-worktrees]] + STATE Notes + root CLAUDE.md + the enforcing helper.
**Next / open:** (none) — concurrent-session hazard fully mitigated and documented end-to-end. Awaiting next task.

### [02:28] Fix cwd-fragile brain hook launch paths (branch: develop · commits: f69508e)
**Synopsis:** A persisted shell `cd` into `dev-docs/` exposed that `.claude/settings.json` launched all three brain hooks with bare cwd-relative paths; the Stop-hook process then couldn't resolve `stop.js` → `MODULE_NOT_FOUND`, breaking journal gating. Anchored all three commands to `$CLAUDE_PROJECT_DIR` and verified.
**What worked:** Diagnosed before patching — separated *launch-path* fragility (settings.json) from *vault-root* resolution (already robust: `brain-lib.js` `ylsRoot` via `input.cwd`/`__dirname`), so the fix was scoped to settings.json only and the accepted/verified hook scripts were left untouched. Reproduced the exact break (ran Stop hook from `dev-docs/` with `$CLAUDE_PROJECT_DIR` set) → clean exit. Evidence: pre-fix `MODULE_NOT_FOUND` on `dev-docs\.claude\hooks\stop.js`; post-fix `node "$CLAUDE_PROJECT_DIR/.claude/hooks/stop.js"` from cwd=`dev-docs/` → `EXIT=0`; `git commit f69508e` (1 file, +3/-3).
**What did NOT work:** (none) — root cause matched first hypothesis; single-file fix sufficient. Side observation: the AL-5 PII heuristic emits a benign false positive — rule `[0-9a-fA-F]{40,}` matches the 40-char git SHA quoted in this journal's [23:40] block; non-blocking, known AL-5 limitation, regex deliberately NOT tightened (deferred, low value).
**Artifacts:** `.claude/settings.json` — all 3 hook commands → `node "$CLAUDE_PROJECT_DIR/.claude/hooks/<x>.js"`; commit `f69508e` on `develop`. Reusable decision rule: brain hook *launch* paths must be `$CLAUDE_PROJECT_DIR`-anchored; vault-root resolution stays `input.cwd`/`__dirname`.
**Next / open:** Unrelated work in flight — YLS Brain *knowledge-layer* design (lean 4-doc, Approach A) is mid-brainstorm; spec not yet written. Optional deferred: tighten AL-5 regex to exclude git SHAs (low value).

### [13:55] YLS Brain knowledge layer — brainstorm → spec → plan → build (branch: develop · commits: 6b5ec5b 975fd30 dd025f5 c6cd7bf 223ef28 b091ae0 edbd1b4 f41ca58 ec603f1 c6f38d5)
**Synopsis:** Built the user-requested self-maintaining `ylsbrain/knowledge/` layer so any future session learns the whole app (purpose, all features, unbuilt plans, direction) without trusting the 13-month-stale `dev-docs/`. Full superpowers flow: brainstorming → spec (revised after honest self-review) → writing-plans → subagent-driven execution. Shipped 4 cross-linked docs (orientation/superseded/features/roadmap) + `kind: knowledge` schema + mechanical git-diff consolidation clause + structural verifier gate. Code-verified build status (49 BUILT / 12 PARTIAL / 3 UNVERIFIED).
**What worked:** Memory-overrides-docs with an explicit `superseded.md` ledger was the right spine — research surfaced that the docs describe a materially different product (subscriptions/FPD/8-tier roles/integrated-MLM) than reality, and 5 net-new code-vs-doc gaps. Codebase-verified status via 4 parallel research agents (no shared state) then a single synthesis implementer (kept cross-links/counts consistent) then a spec-review fix loop. The red→green verifier gate ([[skills/testing-red-green-verifier-gates]]) made structure objective; the load-bearing independent build-status re-verify caught nothing mislabeled — BUILT claims have real logic, simulated/mock paths honestly kept PARTIAL. Evidence: pre-build `KNOWLEDGE FAIL` (4 docs+3 clauses missing, exit 1) → post-build `KNOWLEDGE OK`; full suite `STRUCTURE/SCHEMA/HOOKS/SETTINGS/KNOWLEDGE OK` → `ALL VERIFY GREEN`; T13 spot-checks — `lib/payments/payment-intent-service.ts:44,110` real `stripe.paymentIntents.create({capture_method})`+`.capture()`; `app/api/accuzip/upload/route.ts:108-159` literally `// we'll simulate` + `Math.random()` (correctly PARTIAL, not BUILT); `app/dashboard/orders/page.tsx:15` mock import (correctly PARTIAL); `auth-service.ts` real `supabase.auth.signInWith*`; AL-5 scan clean ×4; `consolidate.js` exit 0. Applied [[skills/build-safe-destructive-git]] discipline (path-scoped commits, never `git add -A`).
**What did NOT work:** (1) First synthesis pass made `features.md` point to the gitignored disposable dossier for the real 49-feature list — a latent dangling reference; caught in controller spec-review, routed back to the same implementer with the authoritative per-feature table → fixed self-contained (`c6f38d5`, no "dossier" refs). (2) Plan Task 6 assumed the findings dossier was committable but `docs/temp/` is gitignored (Rule 5 scratch) — benign deviation: dossier stays on-disk for synthesis, uncommitted by design, disposable. (3) Verifier greps literal lowercase `where it`; implementer's capitalised heading failed the gate first, fixed with a lowercase parenthetical. No data loss; no dead-ends beyond these.
**Artifacts:** New `ylsbrain/knowledge/{orientation,superseded,features,roadmap}.md`; `.claude/hooks/verify/verify-knowledge.js`; `ylsbrain/CLAUDE.md` (Knowledge-layer schema + mechanical Consolidation clause); `ylsbrain/index.md` (`## Knowledge`). Specs `.claude/plans/feature-ylsbrain-knowledge-layer{,-plan}.md`. Scratch `docs/temp/ylsbrain-knowledge-findings.md` (gitignored, disposable). 10 commits `6b5ec5b…c6f38d5` on develop.
**Next / open:** 5 FLAG-TO-USER conflicts recorded **provisionally** (code-reality=truth) in `superseded.md` F1–F5, pending user confirmation: AccuZip validation simulated vs doc-real; MelissaData no purchase wiring; Mailgun outbound absent; templates static vs DB; user orders dashboard mock. Layer is live, gate GREEN; first mechanical knowledge-reconcile happens at next consolidation.

### [14:00] Consolidation — post knowledge-layer (branch: develop · commits: pending)
**Synopsis:** Post-task consolidation (6 tasks since last). Ran the NEW mechanical knowledge-reconcile clause, promoted one provisional skill on genuine independent reapplication, and found+repaired a real ephemeral brain-state corruption.
**What worked:** The mechanical reconcile clause is decisive, not vibes: `git log --name-only 9e05ba0..HEAD -- app/ app/api/ components/ lib/ supabase/` returned **empty** → zero app-source drift since last consolidation, so `features.md`/`roadmap.md`/`orientation.md` are current by construction; no `needs-reconcile`. Evidence: empty git-log output; `consolidate.js` journal-vs-git gap check = `(none)`; full suite `ALL VERIFY GREEN` post-consolidation. Skill promotion was evidence-gated: [[skills/testing-red-green-verifier-gates]] `provisional`→`established` because it was independently reapplied in a *different* task context (knowledge-layer `verify-knowledge.js` RED→GREEN + route-fix-through-implementer `c6f38d5`), satisfying its own revision-log promotion criterion. [[skills/build-safe-destructive-git]] deliberately kept `provisional` — no destructive git op this session, so its core domain was NOT independently reapplied (honest, not auto-promoted).
**What did NOT work:** Found `ylsbrain/.brainstate/brain.json.lastCoveredTs` poisoned to the string `"SessionStart/PostToolUse/Stop hooks used bare cwd-"` — a wrapped line from the `f69508e` multi-line `git commit -m` body. Root cause: PostToolUse logs Bash `detail` containing newlines into the ledger; `brain-lib.js allLedgerLines` parses each physical line's first tab-field as `ts`, so a wrapped commit-message line becomes a fake ledger timestamp, and `stop.js` then sets `lastCoveredTs` to it. Effect: `unconsumed()` string-compares ISO ts vs the poison ("2026…" < "Session…") so everything looked covered → Stop gate silently failed *open*. Repaired the ephemeral file (valid ISO ts; gitignored, not committed). The deeper fix (sanitize ledger ts parsing / stop multi-line Bash details polluting the ledger) is hook-internals on accepted code — recorded as Next/open, NOT hand-patched mid-goal without review.
**Artifacts:** Promoted `skills/testing-red-green-verifier-gates.md` (+revision-log/evidence), `index.md` skill line `established`, repaired `.brainstate/brain.json` (ephemeral). STATE/log refreshed. No skills deprecated; no new skill distilled (the knowledge-layer build is a one-off structural addition, not a recurring in-taxonomy engineering pattern — forcing a skill would corrupt the abstraction; AL-4).
**Next / open:** (a) Brain-lib hardening: make ledger ts-parsing robust to multi-line Bash `detail` (or strip newlines in PostToolUse detail) so `lastCoveredTs` can't be poisoned — proposed, needs its own reviewed change, do not bundle. (b) 5 FLAG-TO-USER conflicts ([[knowledge/superseded]] F1–F5) still await user confirmation. (c) `build-safe-destructive-git` remains `provisional` pending an independent destructive-git reapplication.

### [17:44] Brainstorm + spec: portable Project Brain System (generalize the brain) (branch: develop · commits: none — artifact external)
**Synopsis:** User asked to generalize this bespoke YLS brain into a reusable, project-specific second-brain system for all their repos, housed outside YLS. Ran full superpowers brainstorming → committed design spec to a new external repo `C:\Users\rob\Documents\Software\_brain\`. Design locked across 7 sections; writing-plans is next.
**What worked:** Section-by-section honest self-review (user repeatedly demanded "review your work") caught real defects each round and converged the architecture by §3: manifest engine|content split; S1↔S3 single-vs-per-file-hash self-contradiction found+resolved; EOL-normalized hashing for Windows autocrlf; the recurring "structural check ≠ semantic/behavioral truth" error (Fix A §4 == gap II §6) was institutionalized as **principle D** + cross-cutting invariants (Node hard-dep, universal fail-open, version-independence) rather than whack-a-moled. Evidence: spec committed `aa02b74` in the `_brain/` repo; spec self-review pass (no placeholders, principle D audited across §5–§9, `.brain.lock`/manifest/`status` enum consistent); decisions captured via AskUserQuestion (vendored installer + `sync`; one-tier + Seam stub; YLS→instance #1; scaffold + guided knowledge runbook; Obsidian optional).
**What did NOT work:** Recurring self-error class — I repeatedly conflated "passes a format/verify check" with "is correct/behaves" and over-claimed completeness ("single change", "ends GREEN", "seed from YLS"); each caught only on explicit honest-review prompting (AL-4-flavored). Mitigation now structural: principle D is a named spec invariant the spec self-review + plan must audit at every verification point. No dead-ends otherwise; architecture stable since §3 (later rounds were rigor, not redesign).
**Artifacts:** External `_brain/` (own git repo, `aa02b74`), spec `_brain/docs/spec-project-brain-system.md` (§1–11). No YLS code/content changed (YLS only becomes instance #1 later, as its own future task). Saved auto-memory [[feedback-tldr-after-long-responses]] (always end long replies with a plain-language TLDR).
**Next / open:** writing-plans → `_brain/docs/plan-project-brain-system.md`; spec user-review gate pending. Implementation §9 step 7 will make YLS instance #1 — a FUTURE YLS task requiring its own journal + consolidation + a §11-style behavioral smoke (principle D: structural verify ≠ behavioral parity), on an isolated filesystem copy first (NOT a worktree — [[concurrent-sessions-worktrees]]).

### [20:56] Built portable _brain system; YLS adopted as instance #1 (branch: develop · commits: ef9b5be)
**Synopsis:** Executed the full `_brain` plan via subagent-driven-development (Phases 0–6.2): extracted+de-YLS'd the engine into the external `C:\Users\rob\Documents\Software\_brain\` repo, built the `brain.ps1` CLI (init/sync/verify/doctor/status/test) + red→green test suite, dogfooded `_brain` as instance #0, then adopted **YLS as instance #1** — YLS now runs the shared engine (inherits the ledger-poisoning + cwd fixes) with its `ylsbrain/` vault content provably byte-unchanged.
**What worked:** Per-task implementer + controller two-stage review with the plan's own red→green gates as the objective spec check caught ~12 real defects before they shipped. The isolated-filesystem-copy gate (Phase 6.1, NOT a worktree — [[concurrent-sessions-worktrees]]) surfaced the 2 adoption prerequisites (missing `ylsbrain/knowledge/.gitkeep`; root `CLAUDE.md` lacked `<!-- brain:pointer -->`) on a throwaway copy *before* the real repo was touched — principle-D behavioral verification (not just structural) is what made adoption safe. Evidence: 6.1 copy → `CONTENT-PRESERVED OK` + protocol-drift whitespace-only; real YLS `brain sync` → 10 engine files, exit 0; `brain verify` → `ALL VERIFY GREEN`; behavioral smoke `stop`=0, `session-start` emits `=== YLS BRAIN ===`; all 4 `ylsbrain/knowledge/*` still `status: current`; commit `ef9b5be` scoped (pre-existing `settings.local.json`/archive draft deliberately left unstaged). Applied [[skills/build-safe-destructive-git]] (path-scoped staging) + [[skills/testing-red-green-verifier-gates]] (the entire build idiom; promoted-`established` skill reused in a 3rd independent context).
**What did NOT work:** Many plan/impl defects, each caught by controller review or a gate and routed back to the implementer (never hand-patched): PowerShell `"$proj`brain"` backtick-escape bug; missing `{{PROJECT_UPPER}}` token (template heading non-generic vs zero-drift); single-letter function names colliding with PS aliases (`R`=Invoke-History, `H`=Get-History); **verify-hooks hooks-dir ping-pong** (source `engine/verify`→`engine/hooks` vs deployed `.claude/hooks/verify`→`.claude/hooks` — fixed twice wrong, then resolved with a dual-path probe); `Invoke-Sync` flat src-path mapping (top-level hooks live in `engine/hooks/`); `Invoke-Doctor` calling the process-`exit`ing verify mid-function; Windows JSON `\\` regex-escape mismatch in the registry check; `Write-Host` not captured vs `Write-Output`; `instances.json` not gitignored. None reached a commit unfixed. The first verify-hooks failure also re-proved principle D (token-clean guard GREEN while the behavioral verifier crashed).
**Artifacts:** External repo `_brain/` (engine/CLI/manifest/templates/tests, instance #0 dogfooded) — its own git history, not in YLS. In YLS: `ef9b5be` (adoption — 9 engine files replaced + `.brain.json`/`.brain.lock`/playbooks/`.gitkeep`/managed-gitignore/root-pointer). Spec+plan at `_brain/docs/`. Principle D ("structural verification ≠ semantic/behavioral truth") was the load-bearing review lens throughout.
**Next / open:** Phase 7 only — `_brain/README.md` + `_brain/CLAUDE.md` + final `brain test` ALL GREEN + tag `v0.1.0` (in the `_brain` repo, not YLS). YLS engine is now sync-managed: future upgrades via `brain sync` from `_brain`; never hand-edit `yls/.claude/hooks/*` (sync would flag as local-mod). Onboarding other projects = `brain init <repo>` (explicit follow-up, out of v1 scope).

### [21:00] Consolidation — post _brain build / YLS instance #1 (branch: develop · commits: pending)
**Synopsis:** Post-task consolidation after the _brain build + YLS adoption. Mechanical knowledge-reconcile clean; no new YLS skill (reasoned); recurring generalizable lessons flagged as Seam candidates; ephemeral brain.json repaired (recurrence now prevented by the adopted engine).
**What worked:** Mechanical reconcile decisive: `git log --name-only 9e05ba0..HEAD -- app/ app/api/ components/ lib/ supabase/` = empty → zero app-source drift, so `[[knowledge/features]]`/`roadmap`/`orientation` stay `current` by construction. Gap check `(none)`. [[skills/testing-red-green-verifier-gates]] (`established`) reused a 3rd independent time (the entire _brain build idiom) — confirms the established status; no revision needed. Evidence: empty reconcile git-log; `consolidate.js` gap `(none)`; `brain verify` YLS `ALL VERIFY GREEN` post-adoption.
**What did NOT work:** `brain.json.lastCoveredTs` was poisoned again pre-consolidation by a pre-adoption multi-line-commit ledger line — but this is now **structurally fixed in the live YLS engine** by the adoption (post-tool-use strips newlines from ledger `detail`; `allLedgerLines` ISO-filters ts). Repaired the ephemeral file as part of this consolidation (precedent: [14:00]); recurrence is prevented going forward, so the long-standing brain-lib-hardening open thread is now **discharged by the adopted engine**.
**Artifacts:** No new skill — the _brain build was an external one-off, not a recurring in-YLS engineering pattern (forcing a skill = anti-pattern, per [14:00] reasoning). **Seam candidates** (generalizable beyond YLS → propose to `_brain/shared/` when cross-project authorized, per §Seam): (1) "structural/token verification ≠ behavioral/semantic truth" as a mandatory review lens (already institutionalized as principle D in the `_brain` spec); (2) "single-char PowerShell function names collide with built-in aliases (`R`=Invoke-History, `H`=Get-History) — use multi-char"; (3) "self-contained verifier scripts must probe both source & deployed layouts for sibling paths". No skills deprecated. brain.json watermark advanced; taskCount reset 0.
**Next / open:** Phase 7 (`_brain` repo: README + CLAUDE.md + `brain test` + tag v0.1.0). Brain-lib ledger-hardening open thread → CLOSED (adopted engine carries the fix). 5 `[[knowledge/superseded]]` F1–F5 flags still await user confirmation (unchanged).


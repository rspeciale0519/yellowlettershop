# yls brain — journal 2026-08-08

### [00:20] AccuZip vendor follow-up analyzed; billing resolved, one blocker narrowed   (branch: bugfix/accuzip-validation · commits: 37281c2)

**Synopsis:** Owner forwarded a second reply from AccuZip support. Verified the
key it references matches what's configured, then updated the local blocker
doc and the tracked progress page with what the reply settled versus what's
still open. No code changed; Phases 2-6 remain parked.

**What worked + Evidence:**
- Confirmed (length + case-insensitive comparison only, no value printed) that
  the key AccuZip support tested is the one already in `.env.local` — no
  credential rotation needed.
- The reply resolved billing: transaction-based, per job, by record count,
  charged only on non-preview download (two pricing examples given — did not
  extrapolate a formula into product-facing docs from two data points).
  Surfaced a new constraint for Phase 2+ design: downloads/uploads must stay
  above 50% per billing period or a penalty invoice applies.
- The reply also confirmed the "CASS API" mentioned in the vendor's first
  email is a separate, unprovisioned AccuZip product (real-time single-record
  validation) distinct from our batch Direct Mail API. Grepped
  `dev-docs/api-accuzip.md` first and confirmed it never documents this
  second product, before concluding the plan's "orders under 3 recipients
  skip validation" default is a hard API constraint, not a design choice.

**What did NOT work / still open:**
- Level 4 vs 5 record-cap question — unanswered across two vendor replies.
  Still the one blocker that could invalidate the AccuZip approach outright.
  Test credentials and webhook/callback support also unanswered — vendor
  routed both to its general support address.

**Artifacts:** `docs/temp/accuzip-blocker.md` (untracked by design, updated);
`.claude/plans/accuzip-run-progress.md` updated and committed `37281c2` on
`bugfix/accuzip-validation`, pushed.

**Next / open:** owner's reply should batch the three still-open items
(level 4/5 cap, test credentials, webhook support) plus confirm which key is
in use. Phases 2-6 stay parked until level 4/5 is answered.

# Journal — 2026-06-13

### [01:10] Feature audit + recommendations report (UTC rollover pointer)
**Synopsis:** Comprehensive done-vs-unbuilt audit completed; full entry was
written under local date in [[journal/2026-06-12]] [01:00] before UTC midnight
rolled the hook's expected file to today.
**What worked:** see [[journal/2026-06-12]] [01:00] — 4 parallel Explore
agents + direct P0 spot-checks; report shipped. Evidence: report exists at
`docs/temp/yls-feature-audit-report.md`; greps in transcript.
**What did NOT work:** journal landed in the prior UTC date file; Stop hook
correctly demanded today's file (known date-based-covering artifact).
**Artifacts:** `docs/temp/yls-feature-audit-report.md`,
`docs/temp/feature-audit-spec.md`, [[journal/2026-06-12]] [01:00].
**Next / open:** user reviews report; Sprint 1 = unbreak checkout.

### [02:00] Differentiators D1-D8 approved + CallRail-vs-Twilio decision   (branch: develop · commits: none — docs/temp only)
**Synopsis:** User approved 8 enhancement recommendations; added as report §9
(D1-D8). Investigated call tracking: CallRail integration chosen over Twilio
build.
**What worked:** Web research (built-in WebSearch after firecrawl 402 +
brave invalid-token failures). Evidence: CallRail ~$50/mo base (5 numbers/
250 min, 1.5-3x w/ overages) vs Twilio ~$1.15/number + $0.0085/min raw but
L-XL build + telecom compliance burden; existing affiliate partnership makes
CallRail revenue-positive → decision recorded in report D6 with
`CallTrackingProvider` interface seam for future Twilio tier.
**What did NOT work:** firecrawl MCP (402 out of credits) and brave MCP
(SUBSCRIPTION_TOKEN_INVALID) — both broken; built-in WebSearch is the
working fallback.
**Artifacts:** `docs/temp/yls-feature-audit-report.md` §9 (D1 PURL/QR, D2 AI
copy, D3 A/B split, D4 template marketplace stats, D5 win-back emails, D6
CallRail, D7 drip sequences w/ per-touch authorize-capture billing, D8
deliverability score = preflight USPS rules + combined checkout score).
**Next / open:** explicitly DO NOT start work yet (user instruction); await
go signal — likely Sprint 1 (unbreak checkout) first.

### [03:30] Production readiness Phase 1 — unbreak checkout (code-complete)   (branch: feature/production-readiness · commits: pending checkpoint)
**Synopsis:** Fixed all four P0 checkout breaks + the newly-found Review-step
pricing-shape mismatch; orders now have list/detail APIs, lifecycle migration,
success + status-timeline pages; both dashboards read real Supabase data.
**What worked:** TDD on three new pure modules. Evidence: `npm test` 89
passing (72 baseline + 17 new); `typecheck:ui` 12 = baseline exactly (same
file set, diffed); touched-file eslint 0 errors; `next build` exit 0;
PaymentStep split 471→336 LOC (+2 extracted components) for the ≤350 gate.
Discovered `calculatePricing` returns `total` while UI reads `totalPrice` —
adapter `lib/orders/pricing-breakdown.ts` now the single mapping point.
**What did NOT work:** Supabase YLS project is PAUSED; auto-mode denied
`restore_project` (owner-level infra) → ALL browser smoke + migration apply
blocked as B6 in `docs/temp/production-blockers.md`; user notified
proactively. Checkpoint lint = delta-gate (736 pre-existing repo errors;
touched files clean) per established precedent.
**Artifacts:** `lib/orders/{pricing-breakdown,proof-inputs,order-summary}.ts`
(+3 test files), `app/api/orders/{proof,route,[orderId]}`,
`supabase/migrations/20260612000000_order_lifecycle.sql`,
`app/orders/[orderId]/{page,success/page}.tsx`,
`components/orders/order-status-badge.tsx`, `steps/payment/*`, rewired
`app/dashboard/{page,orders/page}.tsx`, archived `lib/data-structures.ts`.
**Next / open:** Phase 2 (approval→capture, real AccuZip order path, email
seam); smoke debt clears when owner restores Supabase (B6).

### [04:30] Production readiness Phase 2 — close the loop (code-complete)   (branch: feature/production-readiness · commits: pending checkpoint)
**Synopsis:** Proof→approval→capture loop now exists end-to-end in code:
submit generates the official proof + confirmation email, status page gains
approve/reject, approval captures the Stripe authorization, AccuZip order
path uses the real client (Math.random simulation deleted), and a provider-
agnostic email layer (Resend preferred, Mailgun fallback) backs 4
transactional templates.
**What worked:** TDD again. Evidence: `npm test` 102 passing (+13);
`typecheck:ui` 12 = baseline; touched-file eslint clean (cleared a
pre-existing `any` while there); `next build` exit 0; all files ≤350 LOC.
Capture-failure path records approved-but-uncaptured in status_history
rather than losing the approval.
**What did NOT work:** team-service's sendInvitationEmail stub can never
send (client-side supabase) — email wired into the server invite route
instead. Browser smoke still blocked by B6 (paused Supabase project).
**Artifacts:** `lib/email/{provider,templates,index}.ts` (+test),
`lib/orders/{accuzip-processor,render-proof,generate-proof}.ts` (+test),
`app/api/orders/[orderId]/approve/route.ts`, rewired submit/proof/upload
routes, approve/reject UI on `app/orders/[orderId]/page.tsx`.
**Next / open:** Phase 3 production hardening (persistent queue, shared
rate limit, payment integrity, webhook retry, mapping validation). B3
(email key) + B6 (Supabase restore) remain owner-level.

### [later] Production readiness Phase 3 (partial) — payment integrity, mapping validation, rate-limit foundation   (branch: feature/production-readiness)
**Synopsis:** Landed the Phase-3 items that are pure code (no new-migration
dependency): payment-intent now cancels the orphaned Stripe intent + fails
loudly on DB-insert failure; authorize path releases the hold (cancel) or
refunds (immediate-capture edge) when order-insert fails; column-mapping
gate now requires real address fields mapped, not just an isComplete flag;
distributed rate-limit policy lib + Supabase-RPC store written; hardening
migration authored (persistent jobs, rate-limit RPC, webhook dead letters).
**What worked:** TDD. Evidence: `npm test` 119 passing (+5: rate-limit policy,
column-mapping); `typecheck:ui` 12 = baseline; 0 lint errors touched files;
`next build` exit 0; all files ≤350 LOC.
**What did NOT work:** persistent-queue rewrite + rate-limiter wiring +
webhook retry deferred — they need `20260612010000_production_hardening.sql`
APPLIED to verify, which is blocked by B6 (Supabase paused; restore denied
twice by auto-mode pending explicit user authorization).
**Artifacts:** `lib/rate-limit/{policy,index}.ts` (+test),
`lib/orders/column-mapping.ts` (+test),
`supabase/migrations/20260612010000_production_hardening.sql`, payment
integrity in `app/api/payments/{intent,authorize}/route.ts`, mapping gate in
`components/orders/steps/DataAndMappingStep.tsx`.
**Next / open:** CRITICAL PATH = B6 restore (gates all browser smoke +
migration apply + DB-dependent Phase 3-7 verification). Surfaced to user.

### [later] Phase 3 hardening + 2 security fixes; DB identity RESOLVED; goal paused on owner blocker   (branch: feature/production-readiness · commits 4308b91, d0a151d)
**Synopsis:** Completed Phase 3 code (webhook retry/dead-letter, payment
integrity, mapping gate, rate-limit foundation), fixed 2 review-flagged
security holes, and resolved a project-identity mixup. Production-readiness
goal now paused at its escape clause (past 150-turn bound + only owner-level
blockers remain).
**What worked:** TDD throughout. Evidence: `npm test` 136 passing (72
baseline → +64 across phases); `typecheck:ui` 12 = baseline (0 new);
touched-file eslint 0 errors; `next build` exit 0; files ≤350 LOC.
Authoritative DB-identity proof: decoded the anon+service JWT `ref` claims in
`.env.local` → both `lmtpfgfulkynrktdkgpu` (the app's real DB, live `orders`
table), NOT the dashboard project *named* "YLS" (`jgkkcrnegquqbizfuhqr`).
Security: server-side payment-intent verification (IDOR), fail-closed
rate-limit default.
**What did NOT work:** I mis-identified the Supabase project by its dashboard
NAME instead of reading `.env.local` when the user asked for the project id;
user then edited `.env.local` to the wrong ref (jgkkcr URL + lmtpfg keys =
mismatched). Root-cause lesson: **for "which X does this app use", read the
app's own config (env/JWT), never an external listing's display name.**
Migrations remain unapplied — Claude's CLI/MCP tokens belong to the
jgkkcr-owning account, not the lmtpfg-owning account.
**Artifacts:** `lib/webhooks/retry.ts`, `lib/orders/verify-payment.ts`,
`lib/rate-limit/{policy,index}.ts`, `lib/orders/column-mapping.ts`, payment
integrity in `app/api/payments/{intent,authorize}/route.ts`, webhook
retry+dead-letter in `lib/supabase/webhooks.ts`. Blocker ledger:
`docs/temp/production-blockers.md` B6 (corrected).
**Next / open:** OWNER ACTIONS to resume: (1) revert `.env.local` URL lines to
`https://lmtpfgfulkynrktdkgpu.supabase.co`; (2) provide `sbp_` token / SQL
access for the account owning lmtpfg; then apply 2 migrations + run
chrome-devtools browser smoke (login→list→design→order→pay→proof→status).
Phases 4-7 + D1-D8 remain. B3 (email key) also owner-level.

### [later] DB consolidation investigation + decision (goal paused by owner)   (branch: feature/production-readiness · no code commits this entry)
**Synopsis:** Resolved the DB1-vs-DB2 question definitively and set the
consolidation direction. Goal remains PAUSED per owner ("don't continue until
go"). No schema changes made yet — awaiting DB2 write-access decision.
**What worked:** Authoritative DB identification via direct evidence, not
names. Evidence: decoded anon+service JWT `ref` claims; probed both DBs via
PostgREST service-role (row counts) + Supabase **Management API**
`/database/query` (DB1 token works on jgkkcr → 23 tables; DB2 token
Unauthorized on lmtpfg). Introspected DB1 `orders` columns → proved DB1 &
DB2 have INCOMPATIBLE schemas (DB1 normalized fulfillment model w/
vendor_assignments/tracking/proof_annotations; DB2 = order_state jsonb blob).
DB1 empty (0 rows all 23 tables); DB2 has data (user_profiles=1,
pricing_config=25). DB1's model matches dev-docs' order/fulfillment workflow.
**What did NOT work:** my earlier project-id answer was wrong (named "YLS" =
jgkkcr ≠ the app's configured DB) — user had to correct. ROOT LESSON
(reinforced): identify "which project does this app use" from the app's own
config/JWT `ref`, never an external listing's display NAME. Also: `.env.local`
had BOTH DBs (DB1+DB2 sections, duplicate keys) mid-session.
**Decision:** consolidate INTO DB2's project (lmtpfgfulkynrktdkgpu, YLS-OWNED
account — ownership > schema quality) but ADOPT DB1's superior schema model;
port keepers both ways; refactor Phase 1-3 order code to the normalized model.
**Artifacts:** `docs/temp/production-readiness-status.md` (full snapshot),
`docs/temp/production-blockers.md` (B6 corrected). Found prior YLS-account
token `sbp_v0_8ff3…` in `.claude/settings.local.json` — may unlock DB2 writes.
**Next / open:** owner OK to test/use the settings.local.json token for DB2
DDL; then introspect DB1 → author consolidated migrations → apply to DB2 →
copy data → refactor code → cutover → smoke → resume goal.

### [later] Added D9 (vendor-gated capture) + D10 (AI proof comparison) to scope   (branch: feature/production-readiness · docs only; goal still paused)
**Synopsis:** Owner-approved two new features; documented them — NOT executing
(goal stays paused until "go"). D9: move Stripe capture from customer-approval
to vendor-proof-match (deny → cancel/release, no refund; ~7-day auth-expiry
caveat). D10: AI compares customer designer output vs vendor ready-to-print
proof PDF to automate D9's gate.
**What worked:** Grounded D10 in the two real vendor proofs the owner provided
(`temp/vendor-proof-examples/` LTR+ENV). Evidence: read the LTR proof fully —
vendor renders the exact YLS yellow-letter template w/ per-recipient merged
fields (name/property/phone) + mail-automation marks (data-matrix barcode,
sequence #s 1..1369, blank page-2 backs) that D10 must ignore. ENV proof is
image-only (no pdftoppm here) — not viewed, same approach applies. Design:
hybrid = per-recipient merge-field TEXT verification (the vendor proof's whole
point) + vision fidelity diff (GPT-5.5/Gemini via AI Gateway); confidence-
scored, human-escalation on low confidence (never blind-move money).
**What did NOT work:** (none) — documentation task.
**Artifacts:** `docs/temp/yls-feature-audit-report.md` §9 D9+D10 (+evidence
note, sequencing); `.claude/plans/feature-production-readiness.md` new Phase
6.5 (D9+D10, supersedes Phase-2 capture-on-customer-approval).
**Next / open:** still awaiting owner "go" to (a) execute DB consolidation
(DB1 model into YLS-owned DB2) then (b) reactivate the production-readiness
goal incl. Phase 6.5. D9 depends on DB1-model orders; D10 needs a vision model
key (owner-level) + vendors returning proofs into the system.

### [later] DB consolidation EXECUTED + verified on YLS-owned DB2   (branch: feature/production-readiness · migration committed)
**Synopsis:** Ran the owner-approved consolidation: built DB1's normalized
domain model into the YLS-owned project lmtpfgfulkynrktdkgpu, kept DB2's
keepers + user_profiles + all data, added hardening. Verified entirely
successful. Goal cleared to reactivate.
**What worked:** Backup→transactional-apply→verify, via Supabase Management
API `/database/query` (both DB tokens valid now). Evidence: post-apply
verification query — 33 tables (22 DB1 + 8 DB2 keepers + 3 hardening);
data preserved (pricing_config 25, user_profiles 1, admin_audit_log 1);
orders now DB1-normalized (vendor_assignments present); team-scoped RLS
intact (mailing_lists 6 policies); enums 2; increment_rate_limit RPC present;
user_profiles.team_id augmented. Two failed apply attempts first (both rolled
back cleanly — atomicity confirmed DB2 unchanged): (1) FK-before-PK ordering
→ fixed by emitting PK/unique/check before FKs; (2) DB1 policies referenced
user_profiles.id=auth.uid() but DB2 uses user_id → rewrote subqueries to
user_profiles.user_id + added team_id column (per owner: team-scoped access
was always the plan — preserved in full).
**What did NOT work:** keeping DB2 user_profiles AS-IS broke DB1 team policies
(missing team_id, different key column) → augment + policy-rewrite, not a
naive keep. Reinforced: reconcile RLS key-column conventions when merging
two schemas.
**Artifacts:** `supabase/migrations/20260613000000_consolidate_db1_model.sql`
(generated, committed), `docs/temp/db1-extract/` (extract/build/mq scripts +
db1-raw.json + db2-backup/), `docs/temp/production-blockers.md` B6 RESOLVED.
**Next / open:** REACTIVATE the production-readiness goal. FIRST task = orders-
model refactor (Phase 1-3 code targets the old order_state blob; DB now has
DB1 normalized orders + saved_designs/system_templates naming) — app is
schema-mismatched until refactored. Then Phases 4-7 + 6.5 (D9/D10) + browser
smoke. Migration-history hygiene (older blob-orders migrations now superseded)
is a follow-up.

### [later] Security: fixed PII-exposure RLS carried over from DB1 + regenerated types   (branch: feature/production-readiness · commit pending)
**Synopsis:** Commit security review (CRITICAL x2 + HIGH) flagged over-broad
RLS policies the consolidation faithfully ported from DB1 — they exposed
homeowner PII (mailing lists/records) to ANY caller incl. anonymous. Fixed in
DB2 + regenerated TS types from the consolidated schema.
**What worked:** Full RLS audit (pg_policies) then targeted fix. Evidence:
applied `20260613010000_fix_rls_pii.sql` (dropped mailing_lists/mailing_list_records
"OR is_active=true" SELECT policies + tags/mail_pieces `using(true)`; replaced
with authenticated+owner/team-scoped); post-fix query confirms ZERO over-broad
policies remain on those 4 tables; owner/team SELECT coverage retained (verified
policy lists). `types/supabase.ts` generated via `supabase gen types` (was a
MISSING module the code imported — audit gap now closed) + convenience row
aliases; typecheck 18→14 (4 named-import errors resolved).
**What did NOT work:** DB1's "richer" schema had sloppy RLS (PII-exposing) —
lesson: audit ported RLS, never trust-and-port. Remaining 14 typecheck errors
= 6 pre-existing column-mapping bugs (schema-unrelated) + features targeting
tables not in the consolidated model (assets→user_assets, version-history→
change_history) — real refactor scope.
**Artifacts:** `supabase/migrations/20260613010000_fix_rls_pii.sql`,
`types/supabase.ts`.
**Next / open:** large orders-model code refactor remains (Phase 1-3 order
persistence → normalized orders; assets→saved_designs; version-history→
mailing_list_versions), then Phases 4-7 + 6.5 (D9/D10) + browser smoke.

### [later] Reactivated goal: started orders refactor, hit verification wall → clean green checkpoint   (branch: feature/production-readiness · commit 601bcd5)
**Synopsis:** Began the post-consolidation code refactor. Regenerated types,
refactored the order read-model to normalized columns (correct + tested), then
discovered the refactor is far larger than column renames — the consolidated
DB1 model stores payments INLINE on orders (no payment_intents/transactions
tables), so the whole Phase 1-2 payment layer needs rewriting AND can't be
trusted without browser smoke (needs a test-user credential = owner-level).
Reverted the build-red in-progress orders edits to a clean green checkpoint;
committed the genuinely-done work.
**What worked:** Evidence: `types/supabase.ts` generated from consolidated DB2
(fills a missing imported module — audit gap closed); committed webhook
retry/dead-letter + rate_limit RLS (security-review finding) + Phase 6.5 spec;
gates green at checkpoint — `npm test` 136 passing, `next build` exit 0,
typecheck 14 (honest +2 vs 12: the now-present types module surfaces latent
UserAsset/ChangeHistory mismatches = documented refactor backlog).
**What did NOT work:** keeping a half-done orders refactor would have left a
RED build + knowingly-broken payment routes → reverted to green, preserved the
correct normalized read-model in docs/temp/orders-refactor-wip/. Discipline:
don't ship a large unverified rewrite (goal forbids inflation); the payment-
table mismatch means verification is mandatory before claiming done.
**Artifacts:** committed 601bcd5; `types/supabase.ts` (in 50d383e);
`docs/temp/production-blockers.md` B7 + refactor scope;
`docs/temp/orders-refactor-wip/`.
**Next / open:** OWNER: provide a dev test-user credential + confirm .env.local
cutover to DB2-only → then the orders+payment refactor can be done AND
browser-smoke verified. Full refactor inventory in production-blockers.md.

---

## Task: add test-writer + api-contract-reviewer subagents and gen-test skill

**Synopsis:** Ran /claude-automation-recommender (read-only) then implemented the
subagent + skill recommendations the user accepted. Created two project
subagents and one user-invocable skill, written against the REAL repo
conventions (not the stale ones in existing skills).
**What worked:** Verified actual conventions before writing — routes live in
`app/api/**/route.ts` (NOT `src/app`), tests in `tests/**/*.test.ts` mirroring
source path using `mocha` + node `strict assert` (NOT chai, NOT co-located),
type-check is `npm run typecheck:ui` (NOT the `npm run type-check` the existing
create-api-route/db-migration skills reference), auth = `getAuthContext`
(`@/lib/auth/middleware`) / `requireAdmin` (`@/lib/admin/require-admin`), data
layer = Supabase `@/lib/supabase/server-*` (Prisma is doc-aspirational only).
Evidence: read tests/lib/webhooks/retry.test.ts, tests/setup/register.js
(transpileOnly harness → pure-logic tests only), app/api/accuzip/create-list
route. Matched existing agent frontmatter (name/description-with-examples/
model:sonnet/color) and skill frontmatter (disable-model-invocation:true).
**What did NOT work:** N/A — no blockers. Did NOT run skill-creator's full
eval/benchmark loop (overkill for 3 small config files); applied only its
SKILL.md anatomy guidance.
**Artifacts:** `.claude/agents/test-writer.md`,
`.claude/agents/api-contract-reviewer.md`, `.claude/skills/gen-test/SKILL.md`.
Not committed (left to user).
**Next / open:** `/reload-plugins` (or new session) to register them. NOTE for
later: existing create-api-route + db-migration skills reference a nonexistent
`npm run type-check` script and `src/app/api` paths — stale, worth a fix pass.

---

## Task: fix stale create-api-route + db-migration skills

**Synopsis:** Rewrote both existing project skills, which described a stack the
repo does not have. Investigation found the rot was deeper than the two items
flagged earlier — full rewrites, not patches.
**What worked:** Verified every claim against the live repo before editing.
Findings (all evidenced): (1) NO Prisma anywhere — no `prisma/`,
`prisma.config.ts`, or `@/lib/prisma`; the entire db-migration skill was a
Prisma workflow. Real migrations = hand-written SQL in `supabase/migrations/`
(timestamp-prefixed `YYYYMMDDHHMMSS_name.sql`, `begin;…commit;`, heavily
commented, scoped RLS), types regen to `types/supabase.ts`. (2) Routes live in
`app/api/**` NOT `src/app/api`. (3) Auth helpers in the old skill don't exist:
real user wrapper is `withAuth(async (req,{userId})=>…)` from
`@/lib/auth/middleware` (30 routes); admin is `withAdmin(async (req,admin:
AdminUser)=>…)` from `@/lib/admin/require-admin` — NOT `getAuthContext` /
`@/lib/admin/middleware` / `requireAdmin(req).error`. (4) Data layer = Supabase
(`@/lib/supabase/*`, `createServiceClient` from `@/utils/supabase/service`),
75 routes; Zod in 45. (5) type-check script is `npm run typecheck:ui` not
`npm run type-check`. (6) LOC limit is 350 not the skill's 450.
Rewrote create-api-route (withAuth/withAdmin, Supabase, Zod/unknown-narrowing,
350 LOC, IDOR scoping) and db-migration (Supabase SQL + mandatory scoped RLS,
citing the fix_rls_pii precedent; apply is owner/env-gated → author-and-handoff
if target unconfirmed).
**What did NOT work:** N/A. Deliberately did NOT fabricate an exact migration-
apply command — flagged it owner/env-gated (real DB ref lmtpfgfulkynrktdkgpu)
and told the skill to hand off if creds unconfirmed, since the brain records
migrations as unapplied pending owner.
**Artifacts:** `.claude/skills/create-api-route/SKILL.md`,
`.claude/skills/db-migration/SKILL.md` (rewritten). Not committed.
**Next / open:** None. Both skills now match the real stack. (Sibling: the
test-writer/api-contract-reviewer agents + gen-test skill from the prior entry.)

### [later] Orders refactor + browser smoke VERIFIED on consolidated DB2   (branch: feature/production-readiness · commits cb6f94b, a8842c0)
**Synopsis:** Completed the orders read+write refactor to the normalized
inline-payment model, created a test user, and ran the chrome-devtools browser
smoke against the live consolidated DB2 — login → dashboard → orders list →
order status page all verified with screenshots. Caught+fixed a real auth bug
and a security regression along the way.
**What worked:** Evidence: dev server :3010 on DB2-only env; smoke screenshots
docs/temp/smoke/{01-dashboard,02-orders-empty,03-orders-list,04-order-status}.png;
dev log GET /api/orders 200, /api/orders/[id] 200. Seeded order rendered
"Proof ready" $217.50/250 + derived timeline + Approve/Reject UI. Gates: 143
tests passing, build exit 0, typecheck 14 (no new). Fixes: withAuth cookie
fallback (was Bearer-only → browser fetches 401; smoke caught /api/orders 401→200);
submit re-verifies the PaymentIntent against Stripe (status/owner/amount) —
fixed a CRITICAL IDOR regression from my own refactor (review-flagged).
**What did NOT work:** my submit rewrite dropped payment verification (regression)
— review caught it, re-added Stripe-side. Lesson: when refactoring a security-
critical path, re-assert the invariants, don't assume they carry over.
**Artifacts:** commits cb6f94b (orders refactor) + a8842c0 (auth+security);
`docs/temp/smoke/*.png`; test user yls-e2e@yellowlettershop.test (DB2).
**Next / open:** payment-route refactor (intent/authorize/capture → inline-on-
orders; they still hit nonexistent payment_intents/transactions tables) so the
NEW-order wizard→payment flow works end-to-end; then full-journey smoke; then
Phases 4-7 + D9/D10. Documented in production-blockers.md.

### [04:15] Clear typecheck backlog + B8 hidden-type-debt discovery   (branch: feature/production-readiness · commits: fd726fe, 011afe9)
**Synopsis:** Cleared the 14-error typecheck:ui backlog by recovering the lost
`types/list-builder.ts` (ListCriteria) from git and un-orphaning the assets +
version-history features (migration + types). Then discovered a large hidden
type-debt and cut it 773->707 with two zero-risk waves.
**What worked:** Recovered deleted modules verbatim from `4f7229c^` (the commit
`4f7229c` deleted list-builder/advanced-search/mailing-records/supabase-comprehensive)
— far safer than hand-reconstructing nested types. Wrote migration 20260613030000
(user_assets, asset_share_links, change_history, data_snapshots; team/owner-scoped
PII-safe RLS) from authoritative shapes (20260509000000 + consuming code) and
mirrored the tables+aliases into types/supabase.ts so code typechecks against the
schema WITHOUT touching live prod. Evidence: typecheck:ui 0 (was 14); npm test 143;
every touched file clean under new tsconfig.fullcheck.json; proved 0 of the 707
reference my added symbols. Archived 4 inert -original/-backup files (Rule 1,
imported nowhere) + recovered 3 modules => 773->707.
**What did NOT work:** Applying the migration to live DB2 via Mgmt-API was
correctly DENIED by the auto-mode guardrail (consolidation was authorized; this
follow-on was not). Right call — migrations belong in the deploy pipeline, not
ad-hoc prod writes. So the migration is committed-but-unapplied (B-item).
**Artifacts:** commits fd726fe (backlog+tables) + 011afe9 (archive+recover);
`supabase/migrations/20260613030000_restore_assets_versioning_tables.sql`
(NOT yet applied to live); `tsconfig.fullcheck.json`; `docs/temp/typecheck-debt-finding.md`
(B8: 773->707 baseline, breakdown, recommendation).
**Next / open:** B8 remaining 707 (300 TS2339 + 109 TS2322 + nullability/implicit-any
across ~100 files; missing @/types/mailing-lists, uninstalled react-hook-form +
@stripe/react-stripe-js, next-themes/dist/types) — large type-hardening initiative.
Also still pending: apply 20260613030000 at deploy; full wizard->payment smoke
(needs test-mode keys B1-B5); Phases 4-7 + D1-D10. Awaiting user prioritization.

### [later] B8 consumer-fix wave (accuzip/mailing-lists/templates/contact-cards/team/admin/lib shard)   (branch: feature/production-readiness · commits: none — uncommitted per task)
**Synopsis:** Fixed the TypeScript errors across my assigned B8 shard (~56
errors over 27 files) by adapting CONSUMER code to the fixed `@/types/supabase`
layer — never editing types/ or using any/as-any/ts-ignore. Drove my files
from 56 errors -> 2 (both genuine NEEDS-CENTRAL-FIX blocked by non-shard files).
**What worked:** Ran `npx tsc --noEmit -p tsconfig.fullcheck.json`, filtered to
my files, fixed each at root cause. Evidence: repo total 416 -> 121 errors; my
shard 0 except the 2 documented central blockers; `comm` diff (line-normalized)
vs baseline shows ZERO newly-erroring files (the 2 "new" line-shifted errors are
pre-existing in test-integration/type-verification — non-shard files edited by
concurrent B8 work, not me). Backward-compat verified: the 5 generic-`withAuth`
2-arg consumers (orders/design/team) stayed 0 errors. Key fixes: (1) made
`withAuth<TRoute=unknown>` thread the Next.js route context as a 3rd handler arg
+ awaited `params` (Next 15 params are Promises) across the 6 dynamic routes
that destructured a never-passed 3rd arg — this was ALSO a latent runtime bug
(params was `undefined`); (2) realigned `lib/supabase/server.ts` cookie adapter
to the canonical getAll/setAll form (was deprecated get/set/remove w/ wrong
option types); (3) typed the import-spreadsheet-async transformer -> a real
MappedRecord shape + typed-guard email filter + result-type holders
(Email/Address/Duplicate/CompletenessResult); (4) null-narrowing
(`?? ''`/`?? undefined`/instanceof Error) for nullable DB columns + unknown
catch vars; (5) `Uint8Array.from()` to coerce a `Uint8Array<ArrayBufferLike>`
body to a BodyInit-acceptable fresh ArrayBuffer view (verified the failing-vs-
passing forms in a scratch tsc); (6) admin pages: typed useState holders via
`Parameters<typeof Cmp>[0]['prop']` and dropped the failing `as` casts;
(7) `FileUpload` ref prop -> `RefObject<HTMLInputElement | null>` (React 19).
**What did NOT work / DEAD-CODE+BUGS found:** Two errors are central-type gaps I
cannot fix in-shard: (a) `hooks/use-enhanced-campaigns.ts` sets execution
status 'cancelled' to mirror the DB write in `cancelExecution`, but
`CampaignExecution.status` union (enhanced-campaign-service.ts, not my file)
OMITS 'cancelled' — the type must add it; (b) `app/api/assets/[assetId]/usage`
calls `AssetService.recordAssetUsage` which DOESN'T EXIST and has no backing
table (usage tracking is a stub) — needs a central method+table. Also surfaced
real consumer bugs: team create/accept-invitation passed extra args
(`ownerId`/`userId`) the services ignore (they derive identity from
auth.getUser()); restore-version sent a string `versionId` to a `number`
version_number param (now z.coerce.number); admin/pricing[id] had an unused
`RouteContext` type (left in place — harmless, no noUnusedLocals).
**Artifacts:** edited (uncommitted) — `lib/auth/middleware.ts`,
`lib/supabase/server.ts`, accuzip {status,results,report,upload,create-list}
routes + `lib/api/accuzip-integration.ts` + `lib/api/accuzip/fetch.ts`,
mailing-lists {import-spreadsheet-async,import-spreadsheet,restore-version,
csv-import} routes, templates/[templateId], contact-cards/[id](+set-default),
team/{create,accept-invitation}, admin/pricing/[id], admin dashboard
{analytics,pricing,users,users/[userId]} pages, `lib/mailing-lists-utils/
{mappers,index}.ts`, assets/route, share/[token], `components/media/
file-details-modal.tsx`, `components/profile/personal-info-tab.tsx`,
`components/mailing-list-manager/csv-import-modal/FileUpload.tsx`.
**Next / open:** central fixes for the 2 blockers (add 'cancelled' to
CampaignExecution.status; add AssetService.recordAssetUsage + asset_usage
table). Other B8 shards continue in parallel; repo at 121 errors.

### [05:30] B8 type-debt reconciliation -> ZERO (whole codebase typechecks)   (branch: feature/production-readiness, commits: fd726fe..7517c40)
**Synopsis:** Drove the hidden type-debt 664->0 across ~150 files. Root cause: the app's 970-line hand-authored types/supabase.ts (domain types + rich entity shapes) was REPLACED by the Supabase-generated schema at consolidation, orphaning ~47 imports and cascading 664 errors that next.config `ignoreBuildErrors` had masked.
**What worked:** Phase A - generated Database canonical for tables; restored the non-table domain layer (RBAC/tags/bulk-ops/list-builder/status unions) into types/supabase-domain.ts (recovered from 4f7229c^) + types/domain-extra.ts (contracts derived from consumers); table-backed names alias generated rows. Phase B - 8 parallel general-purpose subagents fixed consumer code by module (strict: NO any/ts-ignore, NO types/ edits, await async server client), each verifying its files to 0 (664->32). Phase C - central fixes (validation/duplicates await; createDesignerDocument(string); CampaignExecution +"cancelled"; AssetService.recordAssetUsage) + archived never-functional scaffolding (dead subscriptions, saved-payment-method UI cluster, dead payment test/legacy routes, incomplete campaign-payment path, unused ui/form.tsx); added `npm run typecheck:full`. Evidence: typecheck:full 0, typecheck:ui 0, npm test 143, next build exit 0 (109 pages). Subagents also fixed many LATENT RUNTIME CRASHES (undefined `value` ref, this.getSupabase() missing, withAuth params undefined in Next15, sidebar.tsx self-referential barrel, wrong modal prop names, response_status vs status column).
**What did NOT work:** Naive verbatim restore of old types netted +17 (hand entity shapes diverge from generated rows) -> split table vs non-table. ignoreBuildErrors:false surfaced Next-build resolution FALSE-POSITIVES (lucide-react `Filter` reported missing though tsc resolves it fine) -> reverted; typecheck:full (tsc) is the authoritative gate. PowerShell Get-ChildItem mishandles [bracket] route dirs -> an early liveness grep missed an importer (wrongly archived payment-integration; tsc caught it; re-verified with ripgrep).
**Artifacts:** commits fd726fe(PhaseA) 260166f(batch1) +B8-complete +7517c40(PhaseC); types/{supabase-domain,domain-extra}.ts; tsconfig.fullcheck.json; archive/* (dead clusters); docs/temp/typecheck-debt-finding.md.
**Next / open:** full wizard->payment browser smoke (user adding test-mode keys); apply migration 20260613030000 at deploy; D1-D10 + Phases 4-7. Documented INCOMPLETE features to build later: saved-payment-methods (needs @stripe/react-stripe-js + PaymentService CRUD), campaign-payment (needs calculateCampaignPricing), asset-usage table.

### [06:10] Full wizard browser smoke (chrome-devtools) — journey verified end-to-end, 9 fixes   (branch: feature/production-readiness, commits: fd726fe..HEAD)
**Synopsis:** Drove the complete new-order wizard in-browser as the e2e test user against live DB2 (Stripe TEST mode, AccuZip auto-mocked in dev). Verified login->dashboard->Step1 data+mapping->Step2 validation->Step3 contact+design->Step4 mailing+campaign->Step5 review+proof->Step6 payment. Found AND fixed 9 real issues along the way.
**What worked:** Evidence (each verified live in-browser + typecheck:full 0 after each): (1) auto-map mis-paired "Address Line 1"->address_line_2 via greedy substring -> rewrote field-outer exact-match (ColumnMappingPage); (2) AccuZip upload 400 — step sent whole ColumnMappingData; mapped to flat {address,city,...} (AddressValidationStep); (3) `accuzip_validation_jobs` table missing -> migration 20260613040000 (applied to DB2); (4) design save 500 — wrote nonexistent `user_designs` -> `saved_designs`+design_data; (5) designer<->order round-trip broken (new-tab + sessionStorage per-tab) -> localStorage + focus rehydrate (verified "Design ready!"); (6) campaignOptions only written on toggle -> default-on-mount (CampaignSetupStep); (7+8) proof "Bucket not found" — self-heal `design-previews` bucket in both proof routes (verified "Design Proof Ready"); (9) applied migration 20260613030000 to DB2 (user_assets/change_history/etc.). Created test user yls-e2e@yellowlettershop.test (known pw via admin API). Pricing engine correct ($0.118/pc postcard).
**What did NOT work:** Actual Stripe authorize->capture NOT reached: 1-recipient order = $0.12 < Stripe $0.50 minimum (test-data, not a bug; intent route + manual-capture config are correct — Stripe rejected the tiny amount). Also the payment "prepare" shows a generic error for sub-min amounts (UX nit). Card-entry mechanism (PaymentStep -> /api/payments/methods + intent + authorize) not yet exercised; saved-card UI cluster was archived (used uninstalled @stripe/react-stripe-js) — checkout card entry needs verification for the final leg.
**Artifacts:** commits (auto-map/columnMapping/accuzip), 7c3cc1f (design-save), d10a7c3 (round-trip+campaign), proof-bucket commit; migrations 20260613030000+040000 APPLIED to DB2; design-previews bucket auto-created. Dev server :3010, browser tab logged in at Step 5.
**Next / open:** to verify actual capture — make order >=$0.50 (bigger list or letter format) + confirm card entry (maybe install @stripe/react-stripe-js) -> authorize -> submit -> proof -> approve -> CAPTURE. Then deploy. Friendlier sub-$0.50 message. Proof-preview + submit-proof both now work.

### [06:45] Capture-leg push — characterized the remaining bug cluster (not reached)   (branch: feature/production-readiness)
**Synopsis:** Per user "push to capture now", tried to get the order >= Stripe's $0.50 min. Pricing is FLAT $0.118/piece regardless of format/postage (placeholder), so only more recipients raise the total. Uploaded a 10-row CSV (auto-map fix works for CSV too — "All columns mapped!", address correct). Did NOT reach actual authorize->capture; surfaced a multi-record-path bug cluster instead.
**What worked:** CSV upload + auto-map verified correct (10 rows, address_line_1 right, no dup). Manual single-record path fully works end-to-end (proven earlier).
**What did NOT work (capture-leg blockers, all REAL):** (1) **Validation staleness** — AddressValidationStep shows the prior accuzipValidation result and never resets when the data source/record count changes (after manual1->csv10 it still showed "Total Records 1"); no re-validate affordance. (2) **CSV-upload validation path** — buildListDataPayload returns `{source:'upload'}` with NO uploadedFileId, and the uploaded CSV is parsed client-side only (not persisted to mailing_list_records), so a real re-validate of an uploaded file has no server records (passing previewData would be a hack, not a fix). (3) **Card entry UNVERIFIED** — couldn't reach it (intent 400'd on $0.12<$0.50); PaymentStep uses /api/payments/methods+intent+authorize (own flow, not the archived cluster), but the actual card-collection UI (Stripe Elements? needs @stripe/react-stripe-js, which is NOT installed) is unconfirmed. (4) UX nit: sub-$0.50 shows generic "Unable to prepare payment processing" not a minimum-order message.
**Artifacts:** docs/temp/smoke-recipients.csv (10 rows). No code changed this entry (declined to ship unverified/hacky fixes).
**Next / open:** focused capture-leg follow-up: persist uploaded CSV to mailing_list_records (+ pass uploadedFileId) OR support a clean multi-record manual path; reset/allow-re-validate on data change; confirm/install card entry (@stripe/react-stripe-js if needed) -> authorize -> submit -> proof(now works) -> approve -> CAPTURE. Pricing realism (flat $0.118) also worth revisiting.

### [13:58] Actual Stripe authorize→CAPTURE reached — checkout closed end-to-end   (branch: feature/production-readiness, commit: 4048a7e)
**Synopsis:** Finished the long-pending capture leg. Drove the wizard to Step 6 as the e2e test user (10-row TX CSV, $1.48 order — above Stripe's $0.50 min), authorized the manual-capture PaymentIntent, submitted the order, generated+stored the proof PDF, then approved the proof to CAPTURE. Found and fixed 2 real production bugs blocking the final two legs; verified the whole authorize→capture chain live on DB2.
**What worked (each verified live + typecheck:full 0, 143 tests):** (1) **Stale-state submit bug** — PaymentStep did updateOrderState({payment:authorized}) then synchronously submitOrder(), but submitOrder/validateCurrentStep are useCallbacks closed over the pre-update orderState (payment.status still 'pending') → "Order validation failed: Payment must be authorized" on EVERY checkout (authorize succeeded in Stripe, client-side submit validation then blocked it — no /api/orders/submit call ever fired). Fix: submitOrder(overrides?) + validateCurrentStep(stateOverride?) merge an effective state; PaymentStep passes the just-authorized payment explicitly. Order then created → /orders/{id}/success ("Your letters are in motion"). (2) **Approve 500 (orders.updated_at)** — approve route wrote orders.updated_at in 3 updates; consolidated schema has no such column (PGRST204). Trace: pre-capture update silently failed (error unchecked), Stripe capture at L82 SUCCEEDED, post-capture update threw on updated_at → 500. So money moved but row stayed submitted/authorized. Fix: drop updated_at from all 3 updates; make capture idempotent (retrieve PI; if already 'succeeded' reconcile from it, don't re-capture); surface the pre-capture approval-write error (throw, not ignore) + set proof_approved_by. Re-clicked Approve → self-healed through the fixed path. Evidence: PI pi_3Thve…voxsryj status=succeeded amount_received=148 (charge ch_3Thve…6h3qV6x); order 1065B687 row → status processing / payment_status captured / amount_captured 1.48 / proof_approved_at+by set; order page shows "Processing" + "Proof approved" timestamp + "In production" current stage.
**What did NOT work / notes:** Outbound email throws EmailNotConfiguredError in dev (no RESEND/MAILGUN keys) but trySendEmail absorbs it — order flow unaffected; prod needs keys set. The first capture attempt is what exposed the partial-failure (capture-before-DB-write) — motivated the idempotency guard (real prod-resilience win, not gold-plating). Dev fast-refresh re-mounts PaymentStep on each edit → fresh PaymentIntent each time (orphaned PIs auto-release in test mode); had to re-select card after reloads.
**Artifacts:** commit 4048a7e (4 files: approve/route.ts, OrderProvider.tsx, PaymentStep.tsx, types/orders.ts). Helpers in docs/temp (gitignored): retrieve-pi.cjs, q-orders-cols.sql, q-order-row.sql, stripe-test-pm.cjs. Order 1065B687 is a completed test order on DB2 (processing/captured).
**Next / open:** checkout is now closed end-to-end. Remaining: persist large uploaded CSVs to mailing_list_records (currently previewData-only); pricing realism (flat $0.118/pc placeholder); friendlier sub-$0.50 message; set email provider keys for prod; wire typecheck:full into CI; D1-D10 + Phases 4-7; deploy. Migration 20260613030000 still applies at deploy.

### [later] Persist order-wizard CSV uploads to mailing_list_records   (branch: feature/production-readiness · commits: none)
**Synopsis:** Closed the long-pending "uploads live only in client previewData" gap. On mapping confirmation for an uploaded file, the wizard now creates an owner-scoped mailing_lists row and bulk-inserts the full (re-parsed) file into mailing_list_records; the resulting mailing_list_id is threaded into orderState.listData.selectedListId so AccuZip validation + fulfillment run on real DB rows. previewData kept as fallback so the verified 10-row happy path is untouched.
**What worked (Evidence: typecheck:full 0, typecheck:ui 0, npm test 167 passing, eslint 0 errors on changed files):** (1) New pure lib lib/orders/upload-list-mapper.ts — mapRowToRecord/mapRowsToRecords (YLS field key → real DB columns: address_line_1→address_line1, zip_code→zip_code, etc.; numeric coercion; unmapped/unsupported source cols incl. `company` preserved verbatim in additional_data jsonb), parseCsv (RFC-4180-ish: quoted fields, "" escapes, CRLF), dbRecordToAccuzip (reads canonical DB columns w/ additional_data fallback). 18 unit tests. (2) New route app/api/orders/upload-list/route.ts (withAuth + Zod, service client, team_id resolved from user_profiles, batched 500-row inserts, orphan-list rollback on insert failure, record_count update). (3) Client helper lib/orders/persist-upload.ts re-reads the full File (preview only had 10 rows) and POSTs. (4) Wired in ColumnMappingStep.handleMappingComplete (fire-and-forget; catch → warn + previewData fallback). (5) AddressValidationStep.buildListDataPayload now prefers selectedListId (persisted rows authoritative) over the uploadedFile→previewData branch.
**What did NOT work / gotchas:** (a) mailing_list_records has NO user_id column — the pre-existing accuzip/upload saved_list branch filtered `.eq('user_id', userId)` (latent: always 0 rows; never hit before because no path set saved_list for uploads). Fixed: verify ownership via parent mailing_lists.created_by (+ team_id via user_profiles) then fetch by mailing_list_id. (b) saved_list records are in canonical DB columns, NOT the wizard's source-column headers, so the route's source-column mapping would've produced empty addresses — added source-aware branch using dbRecordToAccuzip. (c) import-spreadsheet route writes record_data/validation_status columns that don't exist in the consolidated schema — did NOT copy that pattern; mapped to real columns + additional_data instead. (d) TS strict rejected casting the struct to Record<string,unknown> for dynamic column writes — used a typed bag + spread. No migration needed (all target columns exist).
**Artifacts:** Created lib/orders/upload-list-mapper.ts, lib/orders/persist-upload.ts, app/api/orders/upload-list/route.ts, tests/lib/orders/upload-list-mapper.test.ts. Modified app/api/accuzip/upload/route.ts, components/orders/steps/ColumnMappingStep.tsx, components/orders/steps/AddressValidationStep.tsx. No commit (per task). Integration field: orderState.listData.selectedListId carries the persisted mailing_list_id (also flows into orders.metadata.order_state at submit).
**Next / open:** Browser-smoke the full upload→persist→validate path on DB2 (logic+gates evidenced; UI path not yet manually verified this session). XLSX uploads still fall back to previewData (parseCsv is CSV-only; client has no xlsx dep). Remaining production-readiness items unchanged (pricing realism, email keys, CI, D1-D10, deploy).

### [15:24] Production-readiness hardening pass + full re-smoke → 100% green   (branch: feature/production-readiness, commits: 28e50d6,15b7840,821eebc,80d65f7)
**Synopsis:** Per owner "finish the goal, don't stop until E2E+smoke pass 100%", closed the remaining production gaps (security findings + hardening + mock-surface honesty + functional persistence) and re-smoked the entire customer journey on a fresh order to capture. Scope explicitly EXCLUDED D1-D10 differentiators (net-new, need external accounts) — told owner.
**What worked (evidence — all green):** typecheck:full 0, 167 Mocha tests, clean `next build` (full route table, archived routes absent), full chrome-devtools smoke of order CAF16D73. Shipped: (1) **proof PII fix** — design-previews bucket → PRIVATE (migration 20260613060000, applied DB2), proof_urls stores PATH, served via 30-min signed URLs (lib/orders/proof-storage.ts); verified signed→200, public→400. (2) fail-closed PI ownership in submit+authorize (was `metadata?.user_id && …` → skipped when absent). (3) **pricing reads admin pricing_config** (lib/orders/pricing-config.ts) with constants fallback — pure fn unchanged so tests hold; live $1.18 for 4x6×10. (4) real Supabase **TOTP 2FA** (lib/auth/mfa.ts + components/security/two-factor-auth.tsx) — verified real QR+secret enroll, removed fake recovery codes. (5) **DB-backed job queue** (background_jobs table; lib/jobs/* now async, callers awaited). (6) **CSV upload persistence** to mailing_list_records (app/api/orders/upload-list + lib/orders/upload-list-mapper/persist-upload) — verified 10 records persisted, validation routes via saved_list ("MLM SELECT"). (7) error boundaries (global-error + orders + dashboard). (8) archived dead RBAC cluster + mock api-keys/notifications pages (+nav). Capture confirmed: PI succeeded, amount_received 118, charge ch_3Thx1V…; order row processing/captured/1.18. Parallelized 3 builds via background subagents (job-queue, CSV, 2FA), integrated clean.
**What did NOT work (all fixed mid-smoke):** (a) **Consolidation gap** — trigger create_audit_log_entry() writes mailing_list_audit_log, a table that was never created → every mailing_list_records insert 500'd (42P01). Created+applied migration 20260613070000. Caught only because the new CSV-persist path is the first code to insert there. (b) `next build` first failed with phantom-route PageNotFoundError (/.identity etc.) — a .next contention artifact from the dev server running concurrently; killing dev + clearing .next → clean build. (c) Prod-write guardrail correctly blocked auto-applying migrations on "finish the goal" — required explicit owner authorization (got it, batched 2).
**Artifacts:** commits 28e50d6 (security), 15b7840 (pricing/2FA/jobs/CSV/boundaries), 821eebc (mock-page removal), 80d65f7 (audit-log migration + RBAC deletions). Migrations applied to DB2: 20260613060000 (proof bucket private), 20260613070000 (mailing_list_audit_log). Test orders on DB2: 1065B687 + CAF16D73 (both processing/captured). Dev server :3010 restarted post-build.
**Next / open:** D1-D10 differentiators (net-new, owner external accounts needed); deploy; deferred minors — project CLAUDE.md stale-ref cleanup, subscriptions archive (entangled in payments barrel), template gallery DB-backing (=D4), XLSX upload parse (CSV-only today), job-queue orphan-reaper. Wire typecheck:full + build into CI.

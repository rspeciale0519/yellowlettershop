# @react-three/fiber@9 and @react-three/drei@10 declare React 19 peers but run
# correctly on this repo's React 18.3.1 (proven on branch new-ui-001, where the
# 3D letter inspector ships on the same versions). Without this flag npm
# refuses to install them (ERESOLVE).
legacy-peer-deps=true
